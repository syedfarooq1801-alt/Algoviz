// One line per problem: what the naive/brute-force attempt looks like, and
// what it costs. This is the thing you should be able to state in the first
// 60 seconds of an interview before optimising — "here's the obvious way and
// why it's too slow" is what earns the right to talk about the pattern.
//
// Keyed by Problem.id from data/problems.ts. A few problems have no genuinely
// worse approach (the direct traversal IS optimal); those say so explicitly
// rather than inventing a strawman.

export const BRUTE_FORCE: Record<string, string> = {
  // ---- Arrays & Hashing ----
  "contains-duplicate": "Compare every pair of elements with nested loops — O(n²) time, O(1) space.",
  "valid-anagram": "For each character in s, scan t for an unused match and cross it off — O(n²).",
  "two-sum": "Try every pair (i, j) and check whether nums[i] + nums[j] == target — O(n²).",
  "ransom-note": "For each character in the note, scan the whole magazine for an unused copy — O(n·m).",
  "isomorphic-strings": "For every pair of positions, verify the character mapping stays consistent — O(n²).",
  "group-anagrams": "Compare every string against every other, re-sorting both on each comparison — O(n²·k log k).",
  "top-k-frequent": "Count each value by rescanning the array for it, then sort all counts and take k — O(n²).",
  "encode-decode-strings": "Join with a rare delimiter and split on it to decode — O(n), but silently breaks the moment any input string contains that delimiter.",
  "product-except-self": "For each index, loop over every other element multiplying them together — O(n²).",
  "valid-sudoku": "For each filled cell, scan its entire row, column, and 3×3 box for a duplicate — 81 cells × 27 checks.",
  "longest-consecutive": "For each element, repeatedly search the whole array for value+1 to extend the streak — O(n³) unsorted.",
  "first-missing-positive": "For k = 1, 2, 3, … scan the entire array checking whether k is present — O(n²).",

  // ---- Two Pointers ----
  "valid-palindrome": "Build a cleaned lowercase copy, reverse it, and compare — O(n) time but O(n) extra space.",
  "move-zeroes": "Repeatedly scan for a zero and shift every later element one slot left — O(n²).",
  "is-subsequence": "Recursively branch on match-or-skip at each character of t — O(2^n) without the greedy insight.",
  "remove-duplicates": "Copy unique values into a second array, then write them back — O(n) time but O(n) extra space.",
  "merge-sorted-array": "Append nums2 onto nums1 and sort the whole thing — O((m+n) log(m+n)), throwing away the sortedness.",
  "two-sum-ii": "Check every pair (i, j) for the target sum, ignoring that the array is sorted — O(n²).",
  "sort-colors": "Call a general comparison sort — O(n log n), versus one pass with three pointers.",
  "three-sum": "Triple nested loop over all index triples, deduping results with a set — O(n³).",
  "container-water": "Compute the area for every pair of lines and keep the maximum — O(n²).",
  "remove-duplicates-ii": "Count each value's occurrences by rescanning, then rebuild keeping at most two — O(n²).",
  "next-permutation": "Generate all permutations in sorted order and return the one after the current — O(n!).",
  "trapping-rain-water": "For each bar, scan left and right to find the tallest wall on each side — O(n²).",
  "count-of-smaller-after-self": "For each index, scan everything to its right and count smaller values — O(n²).",

  // ---- Prefix Sum ----
  "running-sum-1d": "For each index i, re-add every element from 0 to i — O(n²).",
  "find-pivot-index": "For each index, sum the left half and the right half from scratch — O(n²).",
  "range-sum-query": "Loop from i to j on every single query — O(n) per query, O(q·n) overall.",
  "subarray-sum-equals-k": "Sum every subarray (i, j) with nested loops — O(n²) with a running sum, O(n³) fully naive.",
  "subarray-sums-divisible-k": "Sum every subarray and test divisibility by k — O(n²).",
  "continuous-subarray-sum": "Check every subarray of length ≥ 2 for divisibility by k — O(n²).",
  "range-addition": "Apply each update by looping over its entire range — O(k·n) for k updates.",
  "car-pooling": "For every time point on the route, scan all trips to total passengers on board — O(n·range).",

  // ---- Sliding Window ----
  "best-time-stock": "Try every buy/sell pair (i < j) and keep the maximum profit — O(n²).",
  "longest-substring": "For every start index, extend forward tracking seen characters in a set — O(n²).",
  "longest-repeating-replacement": "For every substring, count its most frequent character and check the replacements fit in k — O(n²·26).",
  "permutation-in-string": "For every window of s2 of length |s1|, sort or recount it and compare — O(n·m).",
  "max-points-cards": "Recursively branch take-from-front or take-from-back at each of the k picks — O(2^k).",
  "fruit-into-baskets": "For every start index, extend forward while at most 2 distinct types remain — O(n²).",
  "max-consecutive-ones-iii": "For every subarray, count its zeros and check the count is ≤ k — O(n²).",
  "subarray-product-less-k": "Compute the product of every subarray and compare against k — O(n²).",
  "min-window-substring": "Check every substring of s and test whether it covers all of t — O(n²·m).",
  "sliding-window-max": "For each of the n−k+1 windows, scan all k elements for the maximum — O(n·k).",
  "minimum-window-subsequence": "For every start index in s1, scan forward greedily matching s2 — O(n²).",

  // ---- Stack ----
  "valid-parentheses": "Repeatedly delete adjacent matching pairs \"()\", \"[]\", \"{}\" until the string stops changing — O(n²).",
  "next-greater-element-i": "For each query value, locate it in nums2 then scan right for a bigger one — O(n·m).",
  "implement-queue-using-stacks": "On every pop, move all elements to a second stack, take one, then move them all back — O(n) per operation.",
  "min-stack": "Scan the entire stack on every getMin() call — O(n) per query instead of O(1).",
  "reverse-polish": "Repeatedly find the leftmost operator with two numeric operands and collapse it in place — O(n²).",
  "generate-parentheses": "Generate all 2^(2n) bracket strings and keep the valid ones — O(2^(2n)·n).",
  "daily-temperatures": "For each day, scan forward until you hit a warmer day — O(n²).",
  "next-greater-element-ii": "For each index, walk the array circularly looking for a greater value — O(n²).",
  "car-fleet": "Simulate every car's position time step by time step, merging cars as they collide — O(n·time).",
  "decode-string": "Repeatedly find the innermost k[...] group and expand it in place — O(n²) or worse on nesting.",
  "asteroid-collision": "Repeatedly rescan the array for an adjacent colliding pair and resolve it — O(n²).",
  "largest-rectangle-histogram": "For every pair (i, j), find the minimum height in that span and compute the area — O(n²).",
  "maximal-rectangle": "Try every pair of opposite corners and verify the enclosed rectangle is all 1s — O(m²n²) or worse.",

  // ---- Binary Search ----
  "binary-search": "Scan the array left to right until you find the target — O(n), ignoring the sortedness.",
  "first-bad-version": "Call isBadVersion on 1, 2, 3, … until the first true — O(n) API calls.",
  "search-insert-position": "Walk left to right until you find the value or the first larger one — O(n).",
  "find-first-last-position": "Linear scan for the first occurrence, then keep scanning to the last — O(n).",
  "search-2d-matrix": "Check every cell in the matrix — O(m·n).",
  "koko-bananas": "Try k = 1, 2, 3, … recomputing total hours for each until one fits — O(maxPile·n).",
  "find-min-rotated": "Scan every element tracking the minimum — O(n).",
  "search-rotated": "Linear scan for the target — O(n).",
  "time-based-key-value": "Keep a list per key and scan it linearly for the newest timestamp ≤ the query — O(n) per get.",
  "find-peak-element": "Scan for any index whose value exceeds both neighbours — O(n).",
  "capacity-ship-packages": "Try every capacity from max(weights) upward, simulating the days each time — O(sum·n).",
  "median-two-sorted": "Merge both arrays fully and index the middle — O(m+n) time and space, versus O(log min(m,n)).",
  "split-array-largest-sum": "Try every way to cut the array into k contiguous parts — C(n−1, k−1), exponential.",

  // ---- Linked List ----
  "reverse-linked-list": "Copy all values into an array, reverse it, and write them back — O(n) extra space.",
  "merge-two-sorted": "Collect every node into an array, sort it, and rebuild the list — O(n log n), discarding the sortedness.",
  "linked-list-cycle": "Store every visited node in a hash set and stop on the first repeat — O(n) extra space versus O(1).",
  "palindrome-linked-list": "Copy all values into an array and two-pointer it — O(n) extra space.",
  "reorder-list": "Repeatedly walk to the last node, unlink it, and splice it after the current one — O(n²).",
  "remove-nth-node": "Walk once to measure the length, then walk again to node (length − n) — two passes.",
  "copy-list-random": "For each random pointer, walk from the head counting steps to locate the matching new node — O(n²).",
  "add-two-numbers": "Convert both lists to integers, add them, then rebuild the result list — overflows on long lists.",
  "find-duplicate-number": "Compare every pair of elements — O(n²); a hash set fixes the time but costs O(n) space.",
  "lru-cache": "Keep an array plus a last-used timestamp per key and scan for the oldest on eviction — O(n) per operation.",
  "swap-pairs": "Copy values into an array, swap adjacent entries, and write them back — O(n) extra space.",
  "reverse-linked-list-ii": "Copy the sublist's values into an array, reverse it, and write them back — O(n) extra space.",
  "merge-k-sorted": "Concatenate all k lists into one array and sort — O(N log N); or merge them one at a time, O(k·N).",
  "reverse-k-group": "Copy all values into an array, reverse each block of k, and rebuild the list — O(n) extra space.",
  "lfu-cache": "Store a frequency counter per key and scan every key to find the least-frequent on eviction — O(n) per operation.",

  // ---- Trees ----
  "invert-binary-tree": "Recursively build an entirely new mirrored tree — O(n) extra space instead of swapping in place.",
  "max-depth-tree": "No worse approach exists — you must visit every node, so the direct recursion is already O(n) optimal.",
  "diameter-tree": "For every node, recompute the height of both subtrees from scratch — O(n²).",
  "balanced-tree": "For each node, independently recompute the height of its two subtrees — O(n²).",
  "same-tree": "Serialize both trees to strings and compare — O(n) but with extra space; direct recursion is already O(n).",
  "subtree-of-another": "For every node of root, run a full isSameTree check against subRoot — O(n·m).",
  "symmetric-tree": "Build the fully mirrored tree, then compare it against the original — O(n) extra space.",
  "path-sum": "Enumerate every root-to-leaf path in full and sum each one — O(n·h) once the paths are materialised.",
  "min-depth-tree": "Compute every root-to-leaf depth and take the smallest — O(n) with no early exit on the shallowest branch.",
  "lowest-common-ancestor": "Find the root-to-node path for both nodes, then walk both paths for the last shared node — ignores the BST ordering entirely.",
  "level-order-traversal": "For each depth d, run a separate DFS collecting only the nodes at that depth — O(n·h).",
  "right-side-view": "Run a full level-order traversal storing every level, then keep only each level's last node — O(n) extra storage.",
  "count-good-nodes": "For every node, walk back up to the root comparing against every ancestor — O(n·h).",
  "validate-bst": "For every node, scan its whole left subtree for a larger value and right subtree for a smaller one — O(n²).",
  "kth-smallest-bst": "Do a complete inorder traversal into an array, then index k−1 — O(n) even when k is tiny.",
  "construct-tree-preorder": "For each subtree, linearly scan the inorder array to locate the root's split point — O(n²).",
  "average-of-levels": "Run one separate DFS per level, collecting only the matching nodes — O(n·h).",
  "binary-tree-paths": "Concatenate a fresh string at every step down instead of backtracking one shared buffer — O(n·h) of extra copying.",
  "zigzag-level-order": "Run one DFS per level, then reverse the alternating levels — O(n·h).",
  "path-sum-ii": "Copy the entire current path vector at every recursive call — O(n·h) extra copying versus backtracking one buffer.",
  "boundary-traversal-tree": "Collect every node, then test each one individually for whether it lies on the boundary — O(n²) membership checks.",
  "max-path-sum": "For every pair of nodes, walk the path between them and total it — O(n³) or worse.",
  "serialize-deserialize": "Store the inorder + preorder lists and rebuild by searching for each root — O(n²), and ambiguous when values repeat.",
  "maximum-sum-bst": "For every node, independently verify its whole subtree is a BST and sum it — O(n²).",
  "vertical-order-traversal": "For each candidate column index, traverse the entire tree collecting matching nodes — O(n·width).",

  // ---- Heap / Priority Queue ----
  "kth-largest-stream": "Re-sort the whole stored list on every add() call — O(n log n) per insertion.",
  "last-stone-weight": "Rescan the array for the two largest stones after every smash — O(n²).",
  "k-closest-points": "Sort all n points by distance and take the first k — O(n log n) instead of O(n log k).",
  "kth-largest-array": "Sort the entire array and index n−k — O(n log n) instead of O(n) average with quickselect.",
  "task-scheduler": "Simulate the timeline tick by tick, scanning all 26 counts each tick for the best available task — O(total_time·26).",
  "design-twitter": "On getNewsFeed, gather every tweet from everyone the user follows and sort them all — O(T log T) per feed.",
  "find-median-stream": "Re-sort the full list of numbers on every findMedian() call — O(n log n) per query.",
  "smallest-range-k-lists": "Try every combination of one element per list — O(n^k).",

  // ---- Backtracking ----
  "subsets": "Enumerate all 2^n bitmasks and materialise each subset — same O(2^n·n) output, minus backtracking's shared-path reuse.",
  "combination-sum": "Enumerate every multiset of candidates up to the target and filter — exponential with heavy duplicate work.",
  "permutations": "Generate all n^n index sequences and keep only those using each index once — O(n^n) versus O(n!).",
  "subsets-ii": "Generate all 2^n subsets, sort each, and dedupe via a set — O(2^n·n log n).",
  "combination-sum-ii": "Generate all 2^n subsets, keep those summing to target, and dedupe via a set — O(2^n·n).",
  "word-search": "From every cell, explore every path without pruning on the first character mismatch — explores far more than O(m·n·4^L) needs to.",
  "palindrome-partitioning": "Generate all 2^(n−1) cut placements and check every resulting piece for palindromicity — O(2^n·n).",
  "letter-combinations": "Hard-code one nested loop per digit — same O(4^n) output, but doesn't generalise to arbitrary input length.",
  "combination-sum-iii": "Generate all 2^9 subsets of the digits 1-9 and keep those of size k summing to n — small enough to pass, but wasteful.",
  "n-queens": "Place queens in all n^n position combinations and validate each full board — O(n^n·n²).",
  "sudoku-solver": "Try all 9^(empty cells) digit assignments and validate the whole board each time — astronomically slow without constraint pruning.",

  // ---- Graphs ----
  "find-path-exists": "Repeatedly relax reachability across all edges until nothing changes — O(V·E) versus one DFS.",
  "number-of-islands": "For each land cell, rescan the whole grid to work out which known island it joins — O((mn)²).",
  "clone-graph": "For each node, linearly search the list of already-cloned nodes to find its copy — O(V²) instead of a hash map.",
  "max-area-island": "Flood fill from every land cell without a shared visited set, recounting overlapping islands — O((mn)²).",
  "pacific-atlantic": "For every cell, run a separate search to test whether it can reach each ocean — O((mn)²).",
  "surrounded-regions": "For each 'O' region, flood fill and separately check whether any of its cells touches the border — O((mn)²).",
  "rotting-oranges": "Loop minute by minute, rescanning the entire grid each minute to rot neighbours — O((mn)²).",
  "walls-gates": "From every empty cell, run its own BFS out to the nearest gate — O((mn)²).",
  "course-schedule": "For each course, DFS the whole graph looking for a path back to itself — O(V·(V+E)).",
  "course-schedule-ii": "Try every permutation of courses and check each satisfies all prerequisites — O(n!·E).",
  "graph-valid-tree": "Check connectivity by a fresh search from every pair of nodes, plus a separate cycle hunt — O(V²·(V+E)).",
  "num-connected-components": "For every pair of nodes, run a search to test whether they're connected — O(V²·(V+E)).",
  "redundant-connection": "Remove each edge in turn and test whether the remainder is still a connected tree — O(E·(V+E)).",
  "all-paths-source-target": "Copy the whole path list at every recursive call instead of backtracking — same exponential path count, far more copying.",
  "is-graph-bipartite": "Try all 2^V colourings and validate every edge against each — O(2^V·E).",
  "word-ladder": "DFS every transformation sequence with no visited set — exponential, and revisits the same words endlessly.",
  "critical-connections": "Remove each edge in turn and run a full connectivity check — O(E·(V+E)).",

  // ---- Dynamic Programming ----
  "climbing-stairs": "Plain recursion f(n) = f(n−1) + f(n−2) with no memoisation — O(2^n).",
  "min-cost-climbing": "Recurse both step choices from every index without memoising — O(2^n).",
  "nth-tribonacci": "Naive triple recursion, recomputing the same values over and over — O(3^n).",
  "nim-game": "Recursively simulate every move sequence to see whether a win is forced — O(3^n) versus the one-line n % 4 rule.",
  "house-robber": "Recurse rob-this-house / skip-this-house at every index without memoising — O(2^n).",
  "house-robber-ii": "The same rob/skip recursion, run twice for the two circular cases — O(2^n).",
  "longest-palindromic-substr": "Check every substring for palindromicity character by character — O(n³).",
  "palindromic-substrings": "Test every substring by comparing it against its reverse — O(n³).",
  "decode-ways": "Recursively branch on consuming one digit or two at every position — O(2^n).",
  "coin-change": "Recursively try every combination of coins with no memoisation — exponential in the amount.",
  "max-product-subarray": "Compute the product of every subarray and keep the maximum — O(n²).",
  "word-break": "Recursively try every split point, re-exploring failed suffixes — O(2^n).",
  "longest-increasing-subsequence": "Generate all 2^n subsequences and check each is strictly increasing — O(2^n·n).",
  "partition-equal-subset": "Enumerate all 2^n subsets and test whether any sums to half the total — O(2^n·n).",
  "jump-game-vii": "DFS every reachable index with no visited set or prefix-count trick — O(2^n) worst case.",
  "unique-paths": "Recurse right/down from every cell without memoising — O(2^(m+n)).",
  "minimum-path-sum": "Recurse both directions from each cell without memoising — O(2^(m+n)).",
  "triangle": "Recurse into both children at every level without memoising — O(2^n).",
  "longest-common-subsequence": "Generate all 2^m subsequences of one string and test each against the other — O(2^m·n).",
  "edit-distance": "Recursively try insert/delete/replace at every mismatch without memoising — O(3^(m+n)).",
  "stone-game": "Recursively simulate both players taking either end — O(2^n) versus the parity observation.",
  "buy-sell-cooldown": "Recurse buy/sell/rest at every day with no memoisation — O(3^n).",
  "coin-change-ii": "Recursively branch take-or-skip on each coin without memoising — O(2^n).",
  "target-sum": "Try all 2^n assignments of + and − signs — O(2^n).",
  "interleaving-string": "Recurse taking the next character from s1 or s2 without memoising — O(2^(m+n)).",
  "01-knapsack": "Enumerate all 2^n item subsets and keep the most valuable one that fits — O(2^n·n).",
  "matrix-chain-multiplication": "Try every possible parenthesisation recursively without memoising — Catalan-many, exponential.",
  "best-time-stock-iii": "Try every pair of non-overlapping buy/sell intervals — O(n⁴).",
  "best-time-stock-iv": "Try every set of k non-overlapping transactions — O(n^(2k)).",
  "longest-increasing-path-matrix": "DFS from every cell without memoising, re-walking shared paths repeatedly — exponential.",
  "distinct-subsequences": "Generate all 2^m subsequences of s and count how many equal t — O(2^m·n).",
  "burst-balloons": "Try every order of bursting the balloons — O(n!).",
  "regular-expression-matching": "Recursively try every possible expansion of each '*' with no memoisation — exponential.",
  "max-score-multiplication": "Recurse take-from-left / take-from-right at every step without memoising — O(2^m).",

  // ---- Greedy ----
  "lemonade-change": "Try every combination of held bills to make each customer's change — exponential versus always paying with the largest bill first.",
  "best-time-stock-ii": "Try every set of non-overlapping buy/sell pairs — exponential versus summing all positive day-to-day deltas.",
  "max-subarray": "Sum every subarray (i, j) and keep the maximum — O(n²) with a running sum, O(n³) fully naive.",
  "jump-game": "DFS every reachable index from every position with no memoisation — O(2^n).",
  "jump-game-ii": "Search all jump sequences to find the shortest — exponential without the greedy level-by-level trick.",
  "gas-station": "Try starting at each station and simulate a full loop from there — O(n²).",
  "hand-of-straights": "Try every way to partition the cards into groups of size k — exponential versus always starting from the smallest card.",
  "merge-triplets": "Try every subset of triplets and check their component-wise max equals the target — O(2^n).",
  "partition-labels": "Try every set of cut positions and verify no character spans two parts — exponential versus tracking last-occurrence indices.",
  "valid-parenthesis-string": "Try both interpretations of every '*' — O(2^n) branches.",
  "candy": "Repeatedly sweep the whole array bumping any child who violates the rule, until a pass changes nothing — O(n²).",

  // ---- Intervals ----
  "meeting-rooms": "Compare every pair of meetings for an overlap — O(n²).",
  "insert-interval": "Append the new interval and re-run a full sort-and-merge — O(n log n) instead of a single O(n) pass.",
  "merge-intervals": "Repeatedly scan all pairs, merging any that overlap, until a pass changes nothing — O(n³).",
  "non-overlapping-intervals": "Try every subset of intervals and keep the largest mutually non-overlapping one — O(2^n).",
  "meeting-rooms-ii": "For each meeting, count how many other meetings overlap it and take the maximum — O(n²).",
  "min-interval-query": "For each query, scan every interval for the smallest one containing it — O(q·n).",
  "employee-free-time": "Mark every busy time unit on a flat timeline and scan for gaps — O(range), and hopeless for large or sparse times.",

  // ---- Math & Geometry ----
  "roman-to-integer": "Scan the full numeral value table for each character — O(n·13) versus an O(1) hash lookup.",
  "happy-number": "Iterate the digit-square sum, storing every value seen in a set to detect the loop — O(log n) space versus Floyd's O(1).",
  "plus-one": "Convert the digit array into a single integer, add one, and convert back — overflows for long arrays.",
  "rotate-image": "Write into a fresh n×n matrix at the rotated positions — O(n²) extra space instead of rotating in place.",
  "spiral-matrix": "Keep a separate visited matrix and turn whenever the next cell is visited or out of bounds — O(n²) extra space versus four boundary pointers.",
  "set-matrix-zeroes": "Copy the matrix, then for every 0 in the copy blank that row and column in the original — O(mn) extra space.",
  "pow-x-n": "Multiply x by itself n times — O(n) versus O(log n) by squaring.",
  "multiply-strings": "Repeatedly add num1 to a running total, num2 times — O(num2 · len), astronomically slow for big inputs.",
  "count-primes": "Test each number below n for divisibility by every smaller number — O(n²).",
  "detect-squares": "On each count query, scan every pair of stored points to test for a square — O(n²) per query.",
  "basic-calculator": "Repeatedly locate the innermost parenthesised group, evaluate it, and rewrite the string — O(n²).",

  // ---- Bit Manipulation ----
  "single-number": "For each element, scan the whole array counting its occurrences — O(n²); a hash map fixes time but costs O(n) space.",
  "number-1-bits": "Shift through all 32 positions testing each bit — O(32) versus Brian Kernighan's O(number of set bits).",
  "counting-bits": "Count the bits of each i from 0 to n independently — O(n·32) versus O(n) with DP.",
  "reverse-bits": "Build the reversed value one bit at a time in a 32-iteration loop — O(32); divide-and-conquer masks or caching do better.",
  "missing-number": "For each value 0..n, scan the array checking whether it's present — O(n²).",
  "power-of-two": "Keep dividing by 2 while the number stays even and check you land on 1 — O(log n) versus the O(1) n & (n−1) trick.",
  "sum-two-integers": "With '+' banned, increment a by one, b times — O(b).",
  "reverse-integer": "Convert to a string, reverse it, and parse back — allocates, and needs careful overflow handling on the parse.",
  "bitwise-and-numbers-range": "AND every number from left to right one at a time — O(right − left), times out on wide ranges.",
  "single-number-ii": "Count every value's occurrences in a hash map and return the one appearing once — O(n) time but O(n) space.",
  "single-number-iii": "Hash-map count every value and return the two with a count of 1 — O(n) extra space versus O(1).",
  "maximum-students-exam": "Try all 2^(m·n) seating arrangements and validate each — astronomically slow versus row-by-row bitmask DP.",

  // ---- Tries ----
  "index-pairs-string": "For every dictionary word and every start index, check whether the substring matches — O(n·m·L).",
  "implement-trie": "Store all words in a plain list and scan it linearly for each search/startsWith — O(n·L) per query.",
  "add-search-words": "Keep a plain list and, for a dotted query, compare against every stored word position by position — O(n·L) per search.",
  "replace-words": "For each word in the sentence, test every dictionary root as a possible prefix — O(n·d·L).",
  "longest-word-dictionary": "For each word, verify every one of its prefixes exists by scanning the whole list — O(n²·L).",
  "word-search-ii": "Run a separate full-grid DFS for each word in the dictionary — O(W·mn·4^L).",
  "design-search-autocomplete": "Keep all sentences in a list and, on every keystroke, rescan and re-sort them all by prefix match — O(n log n) per character typed.",

  // ---- Advanced Graphs ----
  "network-delay-time": "Relax all edges V−1 times (Bellman-Ford) — O(V·E) versus Dijkstra's O(E log V).",
  "min-cost-connect-points": "Enumerate every possible spanning tree and keep the cheapest — factorial in the number of points.",
  "cheapest-flights": "DFS every route from source to destination within k stops, with no pruning on cost — exponential.",
  "path-max-probability": "DFS every path from start to end and keep the best probability product — exponential without a priority queue.",
  "evaluate-division": "For each query, DFS the whole graph from scratch multiplying edge weights — O(q·(V+E)) with no caching or union-find.",
  "reconstruct-itinerary": "Try every permutation of tickets and keep the first valid lexicographic one — O(E!).",
  "swim-rising-water": "For each time t = 0, 1, 2, …, run a full BFS to test whether the end is reachable — O(n⁴).",
  "alien-dictionary": "Try all n! orderings of the alphabet and check each is consistent with the word list — O(n!·L).",
  "shortest-path-obstacle": "Search every path without folding the remaining eliminations into the visited state — revisits the same cells exponentially.",
};

/** One-line brute-force description for a problem, if we have one. */
export function getBruteForce(problemId: string): string | undefined {
  return BRUTE_FORCE[problemId];
}
