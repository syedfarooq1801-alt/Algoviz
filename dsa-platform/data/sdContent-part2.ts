// Databases (10) + Networking (8) — 18 concepts
export default {
  "relational-vs-nosql": {
    id: "relational-vs-nosql",
    intuition: "Relational databases enforce schema and support ACID transactions — right for structured, relational data where integrity matters. NoSQL sacrifices some guarantees for scale, flexibility, or access patterns that SQL handles poorly.",
    technicalDetail: "Relational (PostgreSQL, MySQL): tabular schema, JOINs, ACID, normalization, SQL. Scales reads with replicas, writes with careful sharding. NoSQL umbrella: document stores (MongoDB — JSON-like, flexible schema), key-value (Redis, DynamoDB — O(1) access), wide-column (Cassandra, HBase — sorted by partition+clustering key, designed for time-series/high write throughput), graph (Neo4j — traversals on connected data). NoSQL doesn't mean no schema — it means schema enforcement moves to application code. Pick based on access patterns, not popularity.",
    tradeoffs: [
      "Relational: schema-enforced integrity, flexible queries, JOINs — vertical scaling ceiling, rigid schema migration",
      "Document: flexible schema, nested data in one fetch — no joins, duplication required for relational queries",
      "Key-value: O(1) reads/writes, massive scale — no complex queries, key-only access",
      "Wide-column: excellent write throughput, range queries on clustering key — complex data modeling, eventual consistency",
    ],
    realWorldExample: "Airbnb uses PostgreSQL for booking transactions (ACID required — double-booking is catastrophic). They use Elasticsearch for search (full-text doesn't map well to SQL) and Redis for session caching (O(1) key-value access).",
    interviewQuestion: "Would you use a relational DB or NoSQL for a ride-sharing app's trip history?",
    modelAnswer: "SQL for trips: structured data (driver_id, rider_id, start_location, end_location, price), strong consistency needed for billing, complex queries (revenue per driver per week). NoSQL (key-value or document) for real-time location: write-heavy (GPS every 5s per driver), simple access pattern (get current location for driver_id), eventual consistency is fine.",
    commonMistakes: [
      "Choosing NoSQL because 'it scales better' without defining the access pattern — premature optimization",
      "Using a document DB (MongoDB) and then doing JOINs at the application level — use PostgreSQL with JSON columns instead",
      "Normalizing data in a document DB — document DBs favor denormalization for read performance",
    ],
    memoryTrick: "SQL = spreadsheet with enforced column types. NoSQL = folder of JSON files. Spreadsheet = great for accountants. Folder = great for flexible data that changes shape.",
    relatedConcepts: ["acid-vs-base", "sharding", "database-indexing", "nosql-types"],
    cheatSheetLine: "Relational: schema, ACID, JOINs. Document: flexible, nested. Key-value: O(1). Wide-column: write throughput. Graph: traversals. Pick by access pattern.",
  },

  "nosql-types": {
    id: "nosql-types",
    intuition: "NoSQL isn't one thing — it's four fundamentally different models. Each is optimized for a specific access pattern. Using the wrong one is as bad as using SQL when you shouldn't.",
    technicalDetail: "Key-Value (Redis, DynamoDB): hash map at scale. O(1) get/set. Best for caching, sessions, rate-limiting. Document (MongoDB, Firestore): JSON/BSON documents, secondary indexes, flexible schema. Best for user profiles, product catalogs, CMS. Wide-Column (Cassandra, HBase): rows identified by partition key; columns sorted by clustering key. Designed for high write throughput with predictable access patterns. Data modeling: primary key = (partition key, clustering key), must be known at design time. Best for time-series, IoT, activity logs. Graph (Neo4j, Amazon Neptune): nodes and edges. O(1) edge traversal regardless of graph size (vs O(n) JOINs). Best for social graphs, recommendations, fraud detection.",
    tradeoffs: [
      "Key-value: blazing fast — no secondary indexes, queries limited to key lookup",
      "Document: flexible, easy to start — secondary indexes are expensive to maintain at scale",
      "Wide-column: extreme write throughput, good range scans — rigid data model, schema changes costly",
      "Graph: natural connected-data traversals — poor for aggregate queries, niche use cases",
    ],
    realWorldExample: "LinkedIn uses: MySQL for member profiles (relational, ACID), Espresso (document DB) for InMail messages, Kafka (log) for activity streams, and their own Voldemort (key-value) for recommendations. Different data models for different problems.",
    interviewQuestion: "What type of NoSQL database would you use for a social network's friendship graph?",
    modelAnswer: "Graph DB (Neo4j or Amazon Neptune) for friend-of-friend queries and degree-of-separation calculations — these traverse edges, which graph DBs do in O(1) per hop vs O(n) JOINs in SQL. For user profiles (read-heavy, flexible attributes), document DB. For post feeds (time-ordered, high write), wide-column (Cassandra partitioned by user_id, clustered by timestamp).",
    commonMistakes: [
      "Treating DynamoDB as a general-purpose database — its access patterns must be defined upfront; changing them requires table redesign",
      "Using Cassandra for random OLTP queries — wide-column requires known partition key; full scans are expensive",
      "Building a social graph in a relational DB — degree-2 friend queries become multi-level JOINs that don't scale",
    ],
    memoryTrick: "KDWG — Key-Value (speed), Document (flexibility), Wide-Column (write scale), Graph (connections). Memorize one use case for each: Redis=cache, Mongo=catalog, Cassandra=timeseries, Neo4j=social.",
    relatedConcepts: ["relational-vs-nosql", "sharding-strategies", "caching", "eventual-consistency"],
    cheatSheetLine: "KV: O(1), sessions/cache. Document: flexible JSON. Wide-column: write throughput, time-series. Graph: traversals, social/fraud. Pick by access pattern.",
  },

  "oltp-vs-olap": {
    id: "oltp-vs-olap",
    intuition: "OLTP (Online Transaction Processing) serves application writes and point-lookups. OLAP (Online Analytical Processing) runs large analytical queries across historical data. They have opposite optimization targets — never use one for the other's job.",
    technicalDetail: "OLTP: row-oriented storage (PostgreSQL, MySQL). Row = one record stored contiguously. Fast for reading/writing individual rows (INSERT, UPDATE, SELECT by primary key). Normalized schema (3NF). OLAP: column-oriented storage (Redshift, BigQuery, Snowflake). Column = all values for one field stored contiguously. Fast for aggregating millions of rows on a few columns (SUM(revenue) GROUP BY region). Denormalized star/snowflake schema. Typical pipeline: OLTP DB → ETL/ELT → data warehouse (OLAP). Real-time analytics: change data capture (CDC) streams OLTP changes to OLAP in near-real-time.",
    tradeoffs: [
      "Row storage: fast single-row access — full row read even for single-column aggregation",
      "Column storage: fast aggregation over millions of rows — slow individual row writes/updates",
      "Running analytics directly on OLTP DB: blocks application queries — use read replicas or offload to warehouse",
      "ETL pipelines: clean separation — latency (data may be hours old); ELT is faster but messy warehouse schema",
    ],
    realWorldExample: "Uber's analytical pipeline: trips are written to a MySQL (OLTP) cluster. CDC (via Maxwell) streams changes to Kafka, then into Hive/Presto (OLAP). Data scientists query the warehouse, never touching the operational DB. Surge pricing decisions use near-real-time OLAP on ~30-second-old data.",
    interviewQuestion: "How would you design a reporting feature that needs daily revenue by region without impacting your production DB?",
    modelAnswer: "Never run analytics queries on OLTP DB — they compete for locks and degrade response time. Options: 1) Read replica of OLTP DB (simple, near-real-time, still row-oriented). 2) Nightly ETL to a data warehouse (BigQuery, Redshift) — clean separation, better query performance, column-oriented. 3) Streaming CDC for near-real-time warehouse. Choice depends on freshness requirement.",
    commonMistakes: [
      "Running GROUP BY / aggregation queries directly on the production OLTP database — causes lock contention and timeouts",
      "Using a relational DB for time-series analytics — column-oriented stores are 10-100× faster for these workloads",
      "Treating a data warehouse as an OLTP replacement — column stores have high INSERT overhead",
    ],
    memoryTrick: "OLTP = till register (one transaction at a time, fast). OLAP = monthly accountant (scans all receipts, aggregates). Row storage = filing folders. Column storage = filing by attribute (all 'amounts' in one drawer).",
    relatedConcepts: ["database-indexing", "relational-vs-nosql", "replication-strategies", "data-lake-warehouse"],
    cheatSheetLine: "OLTP: row-oriented, point reads/writes, normalized. OLAP: column-oriented, aggregation, denormalized. CDC pipeline connects them. Never mix workloads on same DB.",
  },

  "indexing-internals": {
    id: "indexing-internals",
    intuition: "Understanding how B-Trees and LSM Trees work internally explains why different databases are fast for different workloads — and how to design schema and queries that hit indexes efficiently.",
    technicalDetail: "B-Tree (PostgreSQL, MySQL InnoDB): balanced tree of sorted nodes. Each node is a disk page (4-16KB). O(log n) seek, node reads from disk. Good for reads and mixed workloads. Writes: update B-Tree in place — random I/O. LSM Tree (LevelDB, RocksDB, Cassandra): writes go to in-memory MemTable → flushed to sorted SSTable files on disk. Reads check MemTable, then L0→L1→... SSTables (use Bloom filter to skip levels). Sequential writes only — excellent write throughput. Background compaction merges SSTables. Trade-off: write-optimized but reads need to check multiple levels. Hash Index: O(1) lookup, not range-queryable. Works only if entire index fits in RAM.",
    tradeoffs: [
      "B-Tree: fast reads, OK writes — random I/O on updates (write amplification ~1-2×)",
      "LSM: excellent write throughput (sequential I/O) — read amplification (must check multiple levels), compaction CPU/IO cost",
      "Bloom filter: reduces LSM read I/O dramatically — small false positive rate acceptable",
      "Index size: every secondary index doubles write cost — add only indexes that match real query patterns",
    ],
    realWorldExample: "Cassandra uses LSM: writes go to MemTable+CommitLog first, then flush to SSTables. This makes it excellent for time-series (high write throughput). InnoDB's B-Tree clustered primary key makes PK lookups very fast but secondary index lookups require two lookups (index → PK → clustered row).",
    interviewQuestion: "Why is Cassandra write-fast but read-slower than MySQL for the same data size?",
    modelAnswer: "Cassandra uses LSM Tree — writes are sequential (MemTable flush to SSTable), never random I/O. Reads must check MemTable + multiple SSTable levels — more I/O than a single B-Tree traversal. Bloom filters help skip empty levels but don't eliminate the multi-level check. MySQL's B-Tree writes do random page updates (slower write) but reads are a single balanced-tree traversal (predictable O(log n)).",
    commonMistakes: [
      "Assuming more indexes always help — each extra index adds a B-Tree write on every INSERT/UPDATE",
      "Using Cassandra for random low-latency reads — it's optimized for write throughput, not random reads",
      "Forgetting InnoDB's secondary index double-lookup — secondary index stores PK, then cluster key lookup needed",
    ],
    memoryTrick: "B-Tree = office filing cabinet (organized, easy to find, annoying to update). LSM = inbox pile (easy to add to top, need to search whole pile to read old stuff — compaction = periodically organizing).",
    relatedConcepts: ["database-indexing", "nosql-types", "replication-strategies", "oltp-vs-olap"],
    cheatSheetLine: "B-Tree: sorted, O(log n), random writes. LSM: sequential writes, MemTable→SSTable→compaction, read amplification. Bloom filter = level skip hint.",
  },

  "transactions-isolation": {
    id: "transactions-isolation",
    intuition: "Isolation levels control what anomalies concurrent transactions can see. Higher isolation = fewer anomalies = more locking = lower throughput. Know the four levels and which anomaly each prevents.",
    technicalDetail: "Read Uncommitted: sees other transactions' uncommitted changes (dirty reads). Practically never used. Read Committed: only sees committed data. Prevents dirty reads. Default in most DBs (PostgreSQL, Oracle). Repeatable Read: same row read twice returns same value within transaction. Prevents non-repeatable reads. InnoDB's default. Serializable: transactions appear to execute serially. Prevents phantom reads. Uses range locks or predicate locking. Anomalies by level: Dirty Read (RC prevents), Non-Repeatable Read (RR prevents — same row changes between reads), Phantom Read (Serializable prevents — new rows appear between reads). MVCC (Multi-Version Concurrency Control): readers never block writers — each transaction sees a snapshot at its start time. PostgreSQL uses MVCC for all isolation levels.",
    tradeoffs: [
      "Read Committed: good throughput, common default — non-repeatable reads possible",
      "Repeatable Read: prevents most anomalies — phantom reads still possible (new rows, not changed rows)",
      "Serializable: perfect isolation — highest lock contention, lowest throughput",
      "MVCC: no read-write blocking — old row versions accumulate (vacuum/autovacuum needed to reclaim)",
    ],
    realWorldExample: "PostgreSQL defaults to Read Committed. For financial transfers (debit account A, credit account B), use Serializable or explicit SELECT FOR UPDATE to prevent double-spend. E-commerce inventory check uses Repeatable Read + FOR UPDATE to prevent oversell: read available inventory, decrement, serialize those two operations.",
    interviewQuestion: "Two users check out the last item simultaneously. How do you prevent oversell?",
    modelAnswer: "Pessimistic lock: SELECT quantity FROM inventory WHERE id = ? FOR UPDATE — locks the row until transaction commits. First user gets lock, decrements, commits. Second user waits, then sees quantity=0, returns error. Optimistic: add version column, UPDATE ... WHERE version = expected_version, check rows affected (0 = conflict, retry). Pessimistic is simpler for low-contention scenarios; optimistic for high-read, low-write-conflict scenarios.",
    commonMistakes: [
      "Choosing Serializable for everything — most apps only need Read Committed or Repeatable Read",
      "Not using FOR UPDATE when checking then updating — classic check-then-act race condition",
      "Forgetting that Read Committed doesn't prevent non-repeatable reads — if reading the same row twice matters, use Repeatable Read",
    ],
    memoryTrick: "Levels from weak to strong: Read Uncommitted → Read Committed → Repeatable Read → Serializable. Dirty → Non-Repeatable → Phantom anomalies, each requiring the next level. 'DRSP' = Dirty/Read Committed, Repeatable, Serializable/Phantom.",
    relatedConcepts: ["acid-vs-base", "relational-vs-nosql", "replication-strategies", "saga-2pc"],
    cheatSheetLine: "RC: no dirty reads (default). RR: no non-repeatable reads. Serializable: no phantoms. MVCC: readers don't block writers. FOR UPDATE = pessimistic lock.",
  },

  "replication-strategies": {
    id: "replication-strategies",
    intuition: "How a database replicates data determines its failure behavior, consistency properties, and operational complexity. Knowing single-leader vs multi-leader vs leaderless maps directly to CAP choices.",
    technicalDetail: "Single-Leader (PostgreSQL streaming replication, MySQL binlog replication): all writes go to primary, replicated to replicas. Reads can be served by replicas (eventual consistency). Failover: promote a replica on primary failure (manual or automated via Patroni/Orchestrator). Multi-Leader (Galera Cluster, MySQL Group Replication): multiple primaries in different DCs, each accept writes. Conflict detection required (last-write-wins or application-defined). Leaderless (Cassandra, Dynamo): any node accepts writes. W writes + R reads required where W + R > N nodes for strong consistency. Quorum reads/writes. No single point of failure.",
    tradeoffs: [
      "Single-leader: simple, strong consistency for writes — failover takes 30-60s, write throughput limited to one node",
      "Multi-leader: write availability in multiple regions — conflict resolution complexity, last-write-wins loses data",
      "Leaderless: high availability, no failover — tunable consistency, hinted handoff for temporary failures",
      "Sync vs async replication: sync = zero data loss, write latency doubles; async = fast writes, risk of data loss on primary crash",
    ],
    realWorldExample: "AWS RDS Multi-AZ: synchronous single-leader replication across two AZs. Automatic failover in 60-120 seconds. Aurora: storage layer replicates across 6 nodes in 3 AZs — primary sends log records, not pages. Cassandra: leaderless, configurable quorum. Datastax uses LOCAL_QUORUM to balance consistency with cross-DC latency.",
    interviewQuestion: "Explain the difference between single-leader and leaderless replication for a global database.",
    modelAnswer: "Single-leader: simple, strong writes, but primary is in one region — users in other regions get high write latency (must cross the globe). Failover takes minutes. Leaderless (Cassandra): users write to nearest region's nodes. W=LOCAL_QUORUM means the write is durable in the local region before ack. Cross-region sync is async — reads from another region may be stale. No failover needed — any node handles writes.",
    commonMistakes: [
      "Treating async replication lag as zero — a replica can be seconds or minutes behind primary",
      "Assuming automatic failover is instant — even with automation, failover takes 30-120 seconds (short outage)",
      "Setting W + R <= N in leaderless — this breaks the quorum guarantee and allows stale reads even with high consistency settings",
    ],
    memoryTrick: "Single-leader = one queen, workers obey. Multi-leader = two queens (conflicts!). Leaderless = democracy — quorum vote on truth. W + R > N = majority.",
    relatedConcepts: ["replication", "cap-theorem", "sharding-strategies", "distributed-databases"],
    cheatSheetLine: "Single-leader: simple, write SPOF. Multi-leader: multi-region writes, conflicts. Leaderless: W+R>N quorum, always available. Sync = no data loss, slow.",
  },

  "sharding-strategies": {
    id: "sharding-strategies",
    intuition: "Shard key choice is the most important and hardest-to-change decision in a sharded system. A bad shard key creates hotspots or makes your most common queries cross-shard. Design for your write pattern, not your read pattern.",
    technicalDetail: "Hash sharding: hash(key) % N. Even distribution, good for uniform workloads. Poor for range queries (adjacent keys on different shards). Range sharding: shard by key range (user_id 0-1M → shard1, 1M-2M → shard2). Natural range queries, date-based time-series. Hotspot risk: most traffic goes to the 'latest' shard. Directory sharding: lookup table maps key to shard. Flexible, supports rebalancing. Lookup service is critical path SPOF. Composite shard key (Cassandra): partition key determines shard; clustering key orders data within shard. Cross-shard join: handled at application layer — fetch from multiple shards, aggregate in code. Resharding: moving data from old to new shards. Consistent hashing minimizes data movement (only adjacent shards affected).",
    tradeoffs: [
      "Hash: even distribution, hotspot-resistant — range queries become scatter-gather across all shards",
      "Range: natural range scans — hotspot when recent/popular data concentration on one shard",
      "Consistent hashing: minimal resharding movement — slightly uneven distribution without virtual nodes",
      "Directory: maximum flexibility — lookup service is critical path and must be highly available",
    ],
    realWorldExample: "DynamoDB uses consistent hashing. YouTube's Vitess (MySQL sharding) shards video metadata by video_id (hash). Twitter's Gizzard: directory-based sharding with forwarding trees. Instagram shards user media by user_id: all of a user's photos are co-located, making profile page fetches single-shard.",
    interviewQuestion: "Design the sharding strategy for a URL shortener with 100B URLs.",
    modelAnswer: "Hash shard by short code (hash(code) % N). Short codes are random (already uniform distribution) — no hotspot risk. All operations are single-key lookups (expand: get URL for code; no JOINs needed). Analytics (clicks by URL) can be handled by a separate analytics store. N=100 shards, ~1B URLs per shard at MySQL scale. No range queries needed so hash is optimal.",
    commonMistakes: [
      "Sharding by timestamp: all new data goes to the most recent shard (hotspot) while older shards sit idle",
      "Making the shard key a low-cardinality field (e.g., country): N_shards limited to distinct values in that field",
      "Ignoring cross-shard transaction requirements before choosing shard key — if orders must join with users, shard both by user_id",
    ],
    memoryTrick: "Hash = shuffled deck (even spread). Range = ordered deck (first few cards get all the action if drawing from top). Directory = index card catalog (flexible, but catalog must be available).",
    relatedConcepts: ["sharding", "consistent-hashing", "cap-theorem", "distributed-databases"],
    cheatSheetLine: "Hash: even, no range. Range: natural scan, hotspots. Consistent hash: min resharding. Directory: flexible, lookup SPOF. Co-locate related data on same shard.",
  },

  "distributed-databases": {
    id: "distributed-databases",
    intuition: "Distributed databases spread data and load across multiple machines while presenting a unified interface. The challenge: coordinating writes, maintaining consistency, and handling partial failures without a single point of failure.",
    technicalDetail: "Coordination problem: with multiple nodes, who decides the canonical value? Raft/Paxos consensus protocols elect a leader to coordinate writes — other nodes replicate. Two-Phase Commit (2PC): coordinator asks all participants to prepare, then commit — ensures atomicity across nodes but coordinator is SPOF and blocking (if coordinator dies after prepare, participants are stuck). Spanner (Google): globally distributed, uses TrueTime (atomic clocks + GPS) to assign globally unique timestamps — external consistency across regions. CockroachDB: open-source Spanner-like, uses HLC (Hybrid Logical Clocks). DynamoDB: leaderless, eventual consistency by default, tunable.",
    tradeoffs: [
      "Consensus (Raft): strong consistency, automatic failover — only W-nodes available where W = majority; write latency = slowest quorum member",
      "2PC: atomicity across shards — blocks if coordinator fails; not recommended for long-running distributed transactions",
      "Spanner: externally consistent globally — requires Google infrastructure (TrueTime hardware clocks), expensive",
      "Eventual consistency: high availability, low latency — application must handle stale reads and conflicts",
    ],
    realWorldExample: "Google Spanner runs F1 (ad finance database). 5 replicas across 5 datacenters. External consistency: all committed writes are globally visible in real-time. Used for SQL schemas across 10M shards. CockroachDB offers similar guarantees without Google's custom hardware.",
    interviewQuestion: "How do you ensure a distributed transaction spanning two services is atomic?",
    modelAnswer: "Two main patterns: 2PC (Two-Phase Commit) — coordinator locks resources on both services, then commits or rolls back both. Simple but coordinator is SPOF and participants block on coordinator failure. Saga pattern — sequence of local transactions with compensating transactions for rollback. No distributed lock. Each step publishes an event; next step listens. On failure: run compensating transactions in reverse. Saga preferred for long-running workflows; 2PC for short atomic cross-service updates.",
    commonMistakes: [
      "Using 2PC for long-running workflows — coordinator holds locks for the full duration, blocking concurrent work",
      "Assuming distributed databases handle consistency automatically — even CockroachDB has serializable isolation only if you use the right isolation level",
      "Not designing for partial failure — one node in a distributed DB can fail mid-write; idempotent writes and retry logic are required",
    ],
    memoryTrick: "2PC = conference call where everyone must say 'yes' before proceeding — one silent participant blocks the whole call. Saga = relay race — each runner does their leg; if runner 3 drops baton, runners 1-2 reverse their run (compensating tx).",
    relatedConcepts: ["cap-theorem", "replication-strategies", "saga-2pc", "paxos-raft"],
    cheatSheetLine: "2PC: atomic, coordinator SPOF, blocking. Saga: eventual, compensating tx, no distributed lock. Raft: consensus for leader election. Spanner: global external consistency.",
  },

  "eventual-consistency": {
    id: "eventual-consistency",
    intuition: "Eventual consistency promises that if no new writes occur, all replicas will converge to the same value eventually. It's not 'broken' — it's a deliberate trade for availability and low latency. The key is designing your system to tolerate the inconsistency window.",
    technicalDetail: "Eventual consistency defines the convergence guarantee only — not how long 'eventually' takes (usually milliseconds to seconds). Conflict resolution strategies: Last-Write-Wins (LWW — use timestamps to pick winner; clock skew can lose recent writes), Multi-Version Concurrency (keep all versions, application merges), CRDTs (Conflict-free Replicated Data Types — data structures that automatically merge without conflicts, e.g., G-Counter, OR-Set). Vector clocks: track causality between writes — detect conflicts without wall-clock timestamps. Read-your-own-writes: route each user's reads to the replica they wrote to (or primary) for a brief period after a write.",
    tradeoffs: [
      "LWW: simple implementation — clock skew can silently discard writes that are actually newer",
      "CRDT: conflict-free, auto-merge — limited to specific data types (counters, sets); arbitrary business logic not expressible",
      "Vector clocks: detect all conflicts — complex, must ship vector clock with every write",
      "Monotonic reads: user never sees data go backward — requires sticking to same replica per user (complicates load balancing)",
    ],
    realWorldExample: "Amazon DynamoDB shopping cart: uses a CRDT-inspired merge (add-wins) — if two devices add items simultaneously, both items appear in cart (union). Never silently drop items. Amazon's Dynamo paper (2007) introduced the vector clock approach to e-commerce carts.",
    interviewQuestion: "A user adds a product to their cart on mobile and immediately checks it on desktop — it's not there. Why?",
    modelAnswer: "Cart update wrote to replica A. Desktop read hit replica B, which hasn't received the update yet (replication lag). Solutions: 1) Read-your-own-writes: route reads to the replica the user just wrote to (track by user-session-to-shard mapping). 2) Strong consistency reads: route to primary (higher latency). 3) Client-side optimistic UI: show the add immediately on all clients, sync in background.",
    commonMistakes: [
      "Assuming eventual consistency is 'eventually broken' — it converges reliably; the issue is the window duration, not convergence failure",
      "Using LWW without NTP-synchronized clocks — clock drift can cause write loss",
      "Not handling read-your-own-writes — causes confusing UX where users can't see their own edits",
    ],
    memoryTrick: "Eventual = 'the internet will gossip the news to everyone eventually.' LWW = 'last person to edit Wikipedia wins.' CRDT = 'shared Google Doc — everyone's edits merge automatically.'",
    relatedConcepts: ["cap-theorem", "replication-strategies", "acid-vs-base", "gossip-crdt"],
    cheatSheetLine: "Eventual = converges if no new writes. LWW: simple, clock-skew risk. CRDT: conflict-free merge. Vector clocks: detect causality. Read-your-own-writes: route to correct replica.",
  },

  "multi-region-databases": {
    id: "multi-region-databases",
    intuition: "Multi-region databases distribute data geographically for lower latency (reads from local region), disaster recovery (survive region failure), and data residency compliance. The challenge: cross-region write consistency and latency.",
    technicalDetail: "Active-passive: primary in region A, standby in region B. Reads from local region, writes must go to primary region (cross-region write latency 100-300ms). Failover: promote standby, 1-5 min downtime. Active-active: primaries in both regions. Writes accepted locally — sub-100ms write latency. Conflict resolution required for concurrent writes to the same record. Region-pinned data: user records assigned to their home region; cross-region reads are rare. Global tables (DynamoDB Global Tables): leaderless multi-master, LWW conflict resolution, automatic replication. CockroachDB survival goals: ZONE (survive availability zone failure) or REGION (survive full region failure) configured per table.",
    tradeoffs: [
      "Active-passive: simple, no conflicts — all writes still hit primary region (latency for non-primary users)",
      "Active-active: low write latency globally — conflict resolution complexity, potential data loss with LWW",
      "Cross-region replication lag: typically 50-300ms — users in secondary region may read stale data",
      "Data residency compliance (GDPR): EU users' data must stay in EU — requires region-aware routing and storage",
    ],
    realWorldExample: "Stripe's active-active global deployment: each region has full data copies. Conflict-free writes achieved by assigning each entity (payment, customer) to a home region. A payment created in EU is always routed to EU for writes. US region can read EU data with replication lag. Uber uses region-pinned MySQL clusters — each city's data lives in the nearest region.",
    interviewQuestion: "How would you design a database architecture to serve users globally with sub-50ms reads?",
    modelAnswer: "Read from local region (replicas co-located with users). Write to home region (user account's designated region). Replicate asynchronously from home to other regions. Accept ~100ms replication lag for read-after-write from a different region. Cache frequently-read global data (product catalog, config) in all regions with a short TTL. For strong consistency on critical writes (payments), synchronous multi-region replication with Raft quorum spanning regions — higher latency trade-off.",
    commonMistakes: [
      "Trying to achieve strong consistency across regions without accepting higher write latency — cross-region Raft round-trip is 100-300ms",
      "Ignoring data residency requirements — GDPR fines for EU user data stored outside EU can be severe",
      "Active-active without conflict resolution — concurrent writes to the same record from two regions cause data corruption",
    ],
    memoryTrick: "Active-passive = one office, one remote worker (remote must call main office to commit anything). Active-active = two offices, both accepting work (need weekly sync meeting to resolve conflicting decisions).",
    relatedConcepts: ["replication-strategies", "cap-theorem", "eventual-consistency", "distributed-databases"],
    cheatSheetLine: "Active-passive: simple, cross-region write latency. Active-active: local writes, conflict resolution. Region-pinned: assign entities to home region. CockroachDB: survival goals per table.",
  },

  // ——— NETWORKING ———

  "osi-model": {
    id: "osi-model",
    intuition: "OSI's 7 layers describe how data travels from application to wire. In practice, TCP/IP collapses them to 4 layers. The value for interviews: understanding what each layer does lets you diagnose which layer a problem lives in.",
    technicalDetail: "OSI: L7 Application (HTTP, DNS, SMTP), L6 Presentation (TLS, encoding), L5 Session (session management), L4 Transport (TCP, UDP — ports, reliability), L3 Network (IP — routing between networks), L2 Data Link (Ethernet, MAC — hop-to-hop delivery on same network), L1 Physical (bits on wire). TCP/IP model (practical): Application (L5-7), Transport (L4), Internet (L3), Link (L1-2). Load balancer operates at L4 (routes by IP/port) or L7 (routes by HTTP path/headers). Firewall operates at L3/L4 (IP/port rules). Proxy operates at L7 (reads application headers).",
    tradeoffs: [
      "L4 load balancer: fast (no content inspection) — can't do path-based routing or SSL termination",
      "L7 load balancer: content-aware routing, can inject auth headers — more processing overhead",
      "TCP overhead: 3-way handshake, retransmission — reliability at cost of latency",
      "UDP: no handshake, no retransmission — lower latency for loss-tolerant traffic (video, DNS)",
    ],
    realWorldExample: "AWS ALB (Application Load Balancer) operates at L7 — routes /api to API cluster and /static to S3. AWS NLB (Network Load Balancer) operates at L4 — routes TCP connections by IP/port, no HTTP awareness. For game servers needing UDP, use NLB.",
    interviewQuestion: "What layer does a load balancer operate on and why does it matter?",
    modelAnswer: "L4: routes by TCP/UDP port and IP. No content awareness. Fast. Can't inspect HTTP. L7: routes by HTTP path, headers, host. Can do SSL termination, WebSocket upgrades, A/B routing. More flexible but adds processing. Choose L4 for raw TCP throughput (game servers, IoT). Choose L7 for HTTP microservices routing.",
    commonMistakes: [
      "Confusing L3 (IP routing between networks) and L2 (MAC addressing within network) — L3 router connects networks; L2 switch connects hosts in same network",
      "Thinking OSI is used in practice — TCP/IP model is used; OSI is conceptual",
      "Forgetting TLS operates at L4/5 (above TCP, below HTTP) — a common protocol stack question",
    ],
    memoryTrick: "OSI from top: All People Seem To Need Data Processing. (Application, Presentation, Session, Transport, Network, Data Link, Physical). Or bottom-up: Please Do Not Throw Sausage Pizza Away.",
    relatedConcepts: ["tcp-udp-quic", "tls-handshake", "load-balancing", "proxies-nat"],
    cheatSheetLine: "L7:HTTP, L4:TCP/UDP ports, L3:IP routing, L2:MAC/Ethernet. LB: L4=port-based, L7=content-based. TLS sits between L4 and L7.",
  },

  "tcp-udp-quic": {
    id: "tcp-udp-quic",
    intuition: "TCP guarantees ordered, reliable delivery at the cost of connection overhead. UDP is bare-minimum — fast but unreliable. QUIC (HTTP/3) gets TCP's reliability with UDP's speed by moving handshake logic to the application layer.",
    technicalDetail: "TCP: 3-way handshake (SYN, SYN-ACK, ACK) before data. Guaranteed ordered delivery via sequence numbers. Retransmission on loss. Congestion control. Head-of-line blocking: if packet N is lost, packets N+1, N+2... wait (even if received). UDP: connectionless. No handshake. No ordering. No retransmission. Application handles reliability if needed. Good for video streaming, DNS, gaming (where stale data is worse than no data). QUIC: runs over UDP. TLS 1.3 built in (1-RTT handshake vs TCP+TLS = 2-RTT). Multiple streams within one connection — losing packet in stream 1 doesn't block stream 2 (eliminates head-of-line blocking). Connection migration: connection survives IP change (mobile switching WiFi to 4G).",
    tradeoffs: [
      "TCP: reliable, ordered — 3-way handshake overhead, head-of-line blocking at L4",
      "UDP: zero setup overhead — application must handle loss, ordering, and reliability",
      "QUIC/HTTP3: best of both — but middlebox (firewalls, load balancers) may drop UDP 443 traffic",
      "TCP head-of-line blocking: one lost packet stalls all HTTP/2 streams on the connection — QUIC fixes this per-stream",
    ],
    realWorldExample: "Google uses QUIC for ~70% of traffic to their services. YouTube video loading with QUIC is ~30% faster on mobile networks with packet loss. DNS uses UDP (small query, tolerable to retry if lost). WebRTC (video calls) uses UDP: a dropped video frame is better than waiting for retransmission (visually stale frame > freeze).",
    interviewQuestion: "Why is TCP not ideal for multiplayer games and what would you use instead?",
    modelAnswer: "TCP head-of-line blocking: a lost packet for player position update blocks all subsequent updates until retransmission completes. In a game, that's 100-500ms freeze. Use UDP with custom application-layer reliability: send position updates frequently (loss-tolerant), but for critical events (kill, health) add sequence numbers and ack at application level. Or use QUIC: reliable per-stream without blocking other streams.",
    commonMistakes: [
      "Using TCP for video/audio streaming — tolerate packet loss at application level; retransmission causes worse UX (freeze) than a dropped frame",
      "Thinking QUIC is just faster TCP — it also solves head-of-line blocking and adds connection migration, which are critical for mobile",
      "Forgetting the UDP 443 firewall problem with QUIC — some enterprise firewalls block non-TCP on 443; HTTP/3 falls back to HTTP/2",
    ],
    memoryTrick: "TCP = certified mail (guaranteed delivery, signature required). UDP = postcard (fast, might get lost). QUIC = certified mail via a faster post office (guaranteed + fast).",
    relatedConcepts: ["osi-model", "http-versions", "tls-handshake", "connection-pooling"],
    cheatSheetLine: "TCP: reliable, ordered, 3-way handshake, HoL blocking. UDP: no handshake, no guarantee. QUIC=UDP+TLS+multi-stream, solves HoL blocking, 1-RTT.",
  },

  "http-versions": {
    id: "http-versions",
    intuition: "HTTP evolution solved successive bottlenecks: HTTP/1.1 added persistent connections, HTTP/2 multiplexing, HTTP/3 fixed head-of-line blocking at the transport layer. Know why each version was introduced.",
    technicalDetail: "HTTP/1.0: new TCP connection per request. HTTP/1.1: persistent connections (keep-alive), pipelining (send multiple requests without waiting), but responses must be in order (HoL blocking). Browser limit: 6 connections per domain — developers used domain sharding. HTTP/2: single TCP connection, multiplexed streams (requests/responses interleaved), binary framing, header compression (HPACK), server push. Eliminates need for domain sharding. Still has TCP-level HoL blocking. HTTP/3: runs on QUIC/UDP. Per-stream reliability — packet loss in stream 1 doesn't affect stream 2. 0-RTT connection resumption. Better on high-latency/lossy networks (mobile).",
    tradeoffs: [
      "HTTP/1.1: compatible everywhere — 6-connection browser limit causes queueing for resource-heavy pages",
      "HTTP/2: one connection with multiplexing — TCP HoL blocking still present; middlebox issues with complex upgrade",
      "HTTP/3: best performance especially on lossy networks — UDP firewall blocking in enterprise environments",
      "Server push: proactively send resources — hard to manage cache invalidation, often worse than client-initiated request",
    ],
    realWorldExample: "Google.com is served over HTTP/3 (QUIC) to supported browsers. Chrome sends an Alt-Svc header with QUIC endpoint; subsequent visits use QUIC. Facebook reports ~8% improvement in page load time with HTTP/3 on mobile networks. Major CDNs (Cloudflare, Fastly) support HTTP/3.",
    interviewQuestion: "What problem does HTTP/2 multiplexing solve and what does HTTP/3 still need to fix?",
    modelAnswer: "HTTP/2 multiplexing solves application-level HoL blocking: in HTTP/1.1, if request 2 is slow, request 3 waits on the same connection. HTTP/2 interleaves responses — slow response doesn't block fast ones. But HTTP/2 still uses TCP: if one TCP packet is lost, all 100 HTTP/2 streams on that connection stall waiting for retransmission (TCP-level HoL blocking). HTTP/3 moves to QUIC/UDP with per-stream reliability — a lost packet in one stream doesn't stall other streams.",
    commonMistakes: [
      "Thinking HTTP/2 eliminates all HoL blocking — it eliminates application-layer HoL but TCP-layer HoL remains",
      "Enabling HTTP/2 server push aggressively — browsers have inconsistent support; cache-aware push requires complex 'cache digest' hints",
      "Not enabling HTTP/2 for internal service communication — gRPC already uses HTTP/2; REST APIs should too",
    ],
    memoryTrick: "1.1 = one lane highway (sequential). 2 = multi-lane highway (parallel streams, one blocked lane pauses all if tunnel collapses). 3 = flying (QUIC/UDP — blocked runway doesn't block the air).",
    relatedConcepts: ["tcp-udp-quic", "tls-handshake", "cdn", "connection-pooling"],
    cheatSheetLine: "HTTP/1.1: keep-alive, pipeline, HoL on connection. HTTP/2: multiplexed streams, binary, HPACK, TCP HoL remains. HTTP/3: QUIC/UDP, per-stream reliability, 0-RTT.",
  },

  "tls-handshake": {
    id: "tls-handshake",
    intuition: "TLS (Transport Layer Security) establishes an encrypted channel between client and server. The handshake authenticates the server (via certificate) and negotiates encryption keys. TLS 1.3 cut the handshake from 2 RTTs to 1 RTT.",
    technicalDetail: "TLS 1.2 handshake (2 RTT): Client Hello (cipher suites, TLS version, random nonce) → Server Hello (chosen cipher, certificate, server random) → Client verifies cert (checks CA chain, hostname, expiry) → Key exchange (client encrypts pre-master secret with server public key, or ECDHE) → Both derive session keys from randoms + pre-master secret → Handshake complete. TLS 1.3 (1 RTT): client sends supported key share in Client Hello. Server responds with chosen key share + certificate + already-encrypted Finished. 0-RTT resumption: client sends data in first packet using stored session ticket (replay attack risk — only safe for idempotent requests).",
    tradeoffs: [
      "TLS 1.2: 2 RTT = 200-400ms handshake on high-latency connections — TLS 1.3 halves this",
      "0-RTT resumption: zero extra round trips — replay attack vulnerability for non-idempotent requests",
      "Certificate pinning: client rejects certs not matching expected fingerprint — very secure against MitM, breaks if cert rotates",
      "mTLS (mutual TLS): both sides present cert — strong service identity but certificate management complexity",
    ],
    realWorldExample: "Cloudflare's TLS 1.3 adoption: all traffic to their edge uses TLS 1.3. 0-RTT for returning visitors improves first-byte latency by 100ms on mobile. Google uses mTLS between all internal services — every microservice has a certificate managed by their service mesh (Envoy + SPIFFE).",
    interviewQuestion: "How does TLS protect against a man-in-the-middle attack?",
    modelAnswer: "Server presents a certificate signed by a trusted Certificate Authority (CA). Client verifies the certificate chain up to a trusted root CA stored in the OS/browser. Certificate contains the server's domain name — client checks it matches the requested domain. If an attacker intercepts and presents their own certificate, it won't be signed by a trusted CA (unless they compromise a CA). Certificate Transparency logs allow detection of mis-issued certificates. HSTS pins domains to HTTPS to prevent downgrade attacks.",
    commonMistakes: [
      "Thinking TLS encrypts DNS — DNS is plaintext by default; use DNS over HTTPS (DoH) or DNS over TLS (DoT)",
      "Confusing TLS authentication (is this the real server?) with application authentication (is this user logged in?)",
      "Using self-signed certificates in production — browsers reject them without custom CA configuration",
    ],
    memoryTrick: "TLS = notarized ID check. Certificate = ID. CA = government (trusted issuer). Handshake = client checks ID is real, then both agree on a secret code in public.",
    relatedConcepts: ["tcp-udp-quic", "http-versions", "security-authn-authz", "proxies-nat"],
    cheatSheetLine: "TLS 1.2: 2-RTT. TLS 1.3: 1-RTT, 0-RTT resumption (replay risk). Cert chain → trusted CA. mTLS: both parties present cert. HSTS: no downgrade.",
  },

  "dns-resolution": {
    id: "dns-resolution",
    intuition: "DNS translates human-readable hostnames to IP addresses. It's hierarchical, heavily cached, and often overlooked — but DNS failures or high TTLs are a common cause of slow deployments and outages.",
    technicalDetail: "Resolution chain: browser cache → OS cache → /etc/hosts → local resolver (ISP or 8.8.8.8) → root nameserver → TLD nameserver (.com) → authoritative nameserver → IP returned. Caching: each step caches response for TTL duration. Low TTL = faster propagation but more resolver load. High TTL = cached globally, hard to change IP. Record types: A (hostname → IPv4), AAAA (→ IPv6), CNAME (alias → hostname), MX (mail server), TXT (SPF, DKIM, domain verification), SRV (service location for gRPC/SIP). Round-robin DNS: authoritative nameserver returns multiple IPs in rotating order — primitive load balancing, no health checks. DNS-based load balancing (AWS Route53 health checks): removes unhealthy IPs from responses.",
    tradeoffs: [
      "Low TTL (60s): fast IP change propagation for deployments/failover — higher DNS query volume, slightly higher latency",
      "High TTL (3600s): cached everywhere, minimal resolver load — slow propagation after IP changes",
      "Round-robin DNS: zero infra cost, basic distribution — no health checks, stuck clients until TTL expires if a node fails",
      "CDN CNAME: point domain to CDN's CNAME — CDN handles IP resolution and Anycast routing; lose direct IP control",
    ],
    realWorldExample: "AWS Route53 Latency-Based Routing: Route53 knows which region each IP is in. For a DNS query from a user in Japan, Route53 returns the IP of the Tokyo region server. Health checks remove unhealthy IPs automatically. Netflix uses Route53 for global failover between regions.",
    interviewQuestion: "What TTL would you set for DNS during a major deployment and why?",
    modelAnswer: "24 hours before deployment: drop TTL to 60 seconds. This lets cached resolvers refresh quickly. During deployment: flip IP to new server — all new connections go to new server within 60 seconds. Old TTL (24h) means if you hadn't pre-lowered it, users would be stuck on old IP for 24 hours. After deployment stabilizes: raise TTL back to 300-3600 seconds to reduce resolver load.",
    commonMistakes: [
      "Not pre-lowering TTL before deployment — can't change TTL reactively; cached resolvers ignore new TTL until old TTL expires",
      "Using DNS for health checks alone without a real load balancer — DNS has no health awareness, can't drain connections",
      "Forgetting negative TTL (NXDOMAIN caching) — a failed DNS lookup also gets cached; clearing it requires waiting for negative TTL to expire",
    ],
    memoryTrick: "DNS = phone book. Root = phone book publisher. TLD = section divider (.com). Authoritative = specific listing. TTL = how long you can use the phone book before checking for updates.",
    relatedConcepts: ["cdn", "load-balancing", "tls-handshake", "http-versions"],
    cheatSheetLine: "A/AAAA: IP record. CNAME: alias. MX: mail. TTL: cache duration — pre-lower to 60s before deployment. Round-robin: basic LB, no health checks.",
  },

  "proxies-nat": {
    id: "proxies-nat",
    intuition: "A forward proxy sits between clients and the internet (on behalf of clients). A reverse proxy sits between the internet and your servers (on behalf of servers). NAT translates private IPs to public IPs at network boundaries.",
    technicalDetail: "Forward proxy: client is aware — all traffic routed through proxy. Used for: internet access control (corporate networks), anonymization, caching. Transparent proxy: client unaware — traffic intercepted by network device. Reverse proxy: server-side — clients connect to proxy, unaware of backend servers. Used as: load balancer, TLS termination, API gateway, caching. Nginx as reverse proxy: accepts HTTPS, decrypts, routes HTTP to backends. NAT (Network Address Translation): router replaces source IP/port in packets. Private IP 192.168.1.5:5432 → Public IP 1.2.3.4:45123. Response: router tracks the mapping, translates back. SNAT (source NAT): change source IP. DNAT (destination NAT): change destination — used in load balancers (virtual IP → backend IP).",
    tradeoffs: [
      "Forward proxy: central egress control — single network path, SPOF for internet access",
      "Reverse proxy: hide backend topology, centralize SSL — extra network hop, must be HA",
      "NAT: allows many private IPs to share one public IP — stateful (router must track translations), harder to trace connections",
      "Transparent proxy: users unaware — can intercept HTTPS only if you control the CA cert (corporate MitM)",
    ],
    realWorldExample: "Cloudflare is a reverse proxy for millions of websites — origin servers are never directly exposed to the internet. Corporate networks use forward proxies (Bluecoat, Zscaler) to filter and log all employee web traffic. AWS NAT Gateway: private subnet instances access internet via NAT, not directly routable.",
    interviewQuestion: "What's the difference between a forward and reverse proxy?",
    modelAnswer: "Forward proxy: sits in front of clients. Client configures proxy explicitly. All requests go through it to reach internet. Used for: access control, caching, anonymization. Example: corporate proxy. Reverse proxy: sits in front of servers. Clients don't know it exists — they think they're talking to origin. Used for: load balancing, SSL termination, caching, API gateway. Example: Nginx, Cloudflare.",
    commonMistakes: [
      "Confusing reverse proxy with load balancer — a load balancer is a type of reverse proxy; not all reverse proxies load-balance",
      "Forgetting that NAT is stateful — the translation table can fill up under high connection rates; tune NAT table size",
      "Thinking reverse proxy hides server IPs completely — leak via error pages, redirect headers, or CORS headers revealing internal addresses",
    ],
    memoryTrick: "Forward proxy = client's assistant (goes out to internet on client's behalf). Reverse proxy = server's receptionist (greets clients on server's behalf, routes internally).",
    relatedConcepts: ["load-balancing", "tls-handshake", "api-gateway", "cdn"],
    cheatSheetLine: "Forward proxy: client→proxy→internet. Reverse proxy: internet→proxy→servers. NAT: private IP→public IP. DNAT: dest IP translation (LB virtual IP → backend).",
  },

  "connection-pooling": {
    id: "connection-pooling",
    intuition: "Creating a new database connection is expensive (TCP handshake, auth, TLS negotiation — 20-100ms). Connection pooling reuses a fixed set of open connections, eliminating that overhead for every query.",
    technicalDetail: "Pool maintains N open connections to the database. On query: borrow a connection from pool, execute, return it. No new TCP handshake — query executes immediately. Pool configuration: min/max pool size, connection timeout, idle timeout. PgBouncer (PostgreSQL): proxy-based pooler, handles thousands of app connections with tens of DB connections. Session mode: one DB connection per app session (not shared). Transaction mode: connection returned to pool after each transaction (supports thousands of connections on PostgreSQL's limited max_connections). Statement mode: most aggressive, incompatible with multi-statement transactions. C10K problem: databases have hard limits on max connections (PostgreSQL default 100). Without pooler, 1000 app servers × 10 connections = 10,000 DB connections — DB crashes.",
    tradeoffs: [
      "Small pool: low DB load — app requests queue waiting for available connections under high load",
      "Large pool: more concurrent queries — each idle connection uses DB RAM; DB connection limit hit",
      "PgBouncer transaction mode: handles massive concurrency — no prepared statements, no session-level state",
      "Connection pool per service instance: simple — total connections = services × instances × pool size (can overwhelm DB)",
    ],
    realWorldExample: "Shopify uses PgBouncer in transaction mode in front of PostgreSQL. Their thousands of Rails app workers each think they have a connection, but PgBouncer multiplexes them to 100 real DB connections. Without PgBouncer, Shopify's app servers would exhaust PostgreSQL's max_connections on every traffic spike.",
    interviewQuestion: "Why does connection pooling matter and how does PgBouncer help?",
    modelAnswer: "New DB connections are slow (20-100ms each). At 10,000 RPS, creating a new connection per request adds seconds of overhead. Pool keeps connections open; queries borrow and return immediately. PgBouncer adds another multiplexing layer: 1000 app server connections → PgBouncer → 50 real PostgreSQL connections. PostgreSQL has a max_connections limit (RAM per connection); PgBouncer prevents exhausting it. Transaction mode allows maximum multiplexing since the DB connection is released after every transaction.",
    commonMistakes: [
      "Setting pool size too large — each idle connection uses PostgreSQL RAM (10MB+); DB OOMs on connection spike",
      "Not accounting for total connections = instances × pool_size — 100 app instances × 50 pool size = 5000 DB connections",
      "Using session mode in PgBouncer when transaction mode is sufficient — session mode can't multiplex (one app connection = one DB connection for the whole session)",
    ],
    memoryTrick: "Connection pool = hotel key card system. 500 guests (app requests) but only 50 rooms (DB connections). Front desk (pool) assigns keys, collects when checkout, reassigns to next guest.",
    relatedConcepts: ["tcp-udp-quic", "database-indexing", "latency-vs-throughput", "tail-latency"],
    cheatSheetLine: "Pool reuses open connections (no TCP handshake). PgBouncer: thousands of app conns → tens of DB conns. Transaction mode = max multiplexing. Pool size × instances = total DB connections.",
  },

  "tail-latency": {
    id: "tail-latency",
    intuition: "Tail latency (p99, p999) is what your worst 1% or 0.1% of users experience. At scale, p99 matters more than average — a service making 100 calls has a ~63% chance that at least one call hits p99. Optimizing average while ignoring tail is a common mistake.",
    technicalDetail: "Why tail exists: JVM GC pauses, OS context switches, network jitter, lock contention, cold cache misses. At 100 downstream calls: P(none hit p99) = 0.99^100 ≈ 37% → 63% of aggregate requests hit p99 latency. Hedged requests: send the same request to two replicas after a small delay (e.g., 5ms). Cancel the slower one. Costs ~5% extra server load, eliminates tail. Timeout + retry: timeout at p95 and retry rather than wait for p99+ response. Connection management: keep connections warm — cold connections add TCP+TLS handshake (50-100ms). Load shedding: at saturation, actively reject requests rather than queue them (queueing increases p99 dramatically). Goodput (useful completed requests) can be higher by rejecting at threshold.",
    tradeoffs: [
      "Hedged requests: eliminates tail — increases server load by X% (the hedge overhead)",
      "Aggressive timeouts: faster failure, better p99 — retries can amplify load on struggling services",
      "Load shedding: protects the service — users see errors at peak load instead of slow responses",
      "Thread pool size: too small = queue builds → tail spikes; too large = context switch overhead",
    ],
    realWorldExample: "Google's paper 'The Tail at Scale' (2013): Google uses hedged requests across many internal services. In their experiment, adding hedged requests (redundant requests at 95th percentile) reduced p99 from 10× median to 2× median with only 5% extra server load. Amazon's rule: never let tail latency accumulate across service calls — if p99 is 50ms and you make 100 calls serially, p99 becomes 5000ms.",
    interviewQuestion: "Your service's p99 latency is 500ms but p50 is 20ms. How do you investigate?",
    modelAnswer: "p99 vs p50 gap = tail issue, not general slowness. Investigate: 1) Garbage collection pauses (JVM)? Add GC logging. 2) Lock contention? Thread dumps. 3) DB query: check slow query log — one query may run slow occasionally (lock wait, page fault). 4) Downstream service: trace the slow requests — which downstream call is slow? 5) Use distributed tracing (Jaeger) on p99 requests to find the slow span. Add hedged requests to the identified slow dependency.",
    commonMistakes: [
      "Monitoring and alerting on average latency — a bimodal distribution (most fast, some very slow) looks fine on average",
      "Setting timeouts based on average — set at p95 or p99, not mean; timeouts below p99 cause retry storms",
      "Ignoring fan-out tail amplification — 50 parallel downstream calls at 1% p99 rate = 40% chance of hitting a slow call",
    ],
    memoryTrick: "Fan-out tail math: if each call has 1% chance of being slow, N parallel calls = P(at least one slow) = 1 - 0.99^N. At N=100: 63%. Always measure p99 for fan-out services.",
    relatedConcepts: ["latency-vs-throughput", "connection-pooling", "circuit-breaker", "observability"],
    cheatSheetLine: "p99 matters at scale. Fan-out: P(hit p99) = 1 - (1-p99rate)^N. Hedged requests fix tail. Monitor p50/p95/p99 separately. Never alert on mean latency alone.",
  },
};
