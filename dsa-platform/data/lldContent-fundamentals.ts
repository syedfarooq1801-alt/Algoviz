// LLD Fundamentals: the modelling vocabulary every LLD round assumes you
// already have — OOP pillars, how classes relate, and how to draw it.
// Comes BEFORE SOLID: you can't apply SOLID until you can model at all.

const lldFundamentals = {
  "lld-fundamentals": {
    id: "lld-fundamentals",
    title: "LLD Fundamentals",
    chapters: [
      {
        id: "oop-fundamentals",
        num: 1,
        title: "OOP for LLD — the four pillars in practice",
        blocks: [
          { type: "eli5", text: "Bundle data with the code that works on it (encapsulation). Hide the messy details behind a simple button (abstraction). Let a new thing reuse an old thing (inheritance). Let one word mean the right thing for each type (polymorphism)." },
          { type: "para", text: "Every LLD interview is an OOP modelling exercise wearing a costume. The interviewer says 'design a parking lot'; what they're grading is whether you pick sane classes, give each one clear state and behavior, and wire them together without a God class. The four pillars are the vocabulary for that." },
          { type: "heading", text: "1. Encapsulation — state is private, behavior is public" },
          { type: "para", text: "Data and the operations on it live in the same class, and the raw data is not reachable from outside. The reason isn't secrecy — it's that you can only enforce an invariant you control. If `balance` is public, every caller is now responsible for never making it negative. If it's private behind `withdraw()`, exactly one place is." },
          { type: "pre", text: "// LEAKY — invariant lives in every caller\nstruct Account {\n    double balance;          // anyone can do acc.balance = -500;\n};\n\n// ENCAPSULATED — invariant lives in ONE place\nclass Account {\n    double balance = 0;      // private state\npublic:\n    void deposit(double amt) {\n        if (amt <= 0) throw std::invalid_argument(\"deposit must be positive\");\n        balance += amt;\n    }\n\n    bool withdraw(double amt) {\n        if (amt <= 0 || amt > balance) return false;   // invariant enforced here\n        balance -= amt;\n        return true;\n    }\n\n    double getBalance() const { return balance; }      // read-only view\n};" },
          { type: "heading", text: "2. Abstraction — expose what, hide how" },
          { type: "para", text: "The caller sees a contract, not an implementation. `PaymentGateway::charge()` says what happens; whether it's Stripe, Razorpay, or a test double is nobody's business. In C++ this is a pure-virtual base (or a concept); in Java an interface. Abstraction is what lets you swap implementations later without touching callers — and it's what makes DIP possible." },
          { type: "common-mistake", text: "Confusing abstraction with encapsulation in the interview. Encapsulation = hiding DATA (private members). Abstraction = hiding IMPLEMENTATION (interface vs concrete class). A class can be encapsulated but not abstract, and vice versa. If asked to distinguish them, lead with that one-liner." },
          { type: "heading", text: "3. Inheritance — reuse, but only for genuine is-a" },
          { type: "para", text: "A derived class gets the base's interface and can extend it. The trap: inheritance is the tightest coupling in OOP — the derived class depends on the base's internals, not just its interface. Use it only when the derived type genuinely IS the base type everywhere the base is accepted (that's exactly what LSP formalizes)." },
          { type: "pre", text: "// Composition (has-a) — flexible, swap at runtime\nclass Car {\n    std::unique_ptr<Engine> engine;   // a Car HAS an Engine\npublic:\n    explicit Car(std::unique_ptr<Engine> e) : engine(std::move(e)) {}\n    void start() { engine->ignite(); }\n};\n\n// Inheritance (is-a) — rigid, fixed at compile time\nclass ElectricCar : public Car { /* an ElectricCar IS a Car */ };\n\n// Default to composition. Reach for inheritance only when you need\n// polymorphic substitution through a base pointer/reference." },
          { type: "heading", text: "4. Polymorphism — one call site, many behaviors" },
          { type: "para", text: "The same call dispatches to different code depending on the runtime type. This is the pillar that actually removes the if/else ladders from your design: instead of `if (type == CAR) ... else if (type == BIKE) ...`, you call `spot->getFee()` and each subtype answers for itself. Runtime polymorphism = virtual functions. Compile-time = overloading and templates." },
          { type: "pre", text: "class ParkingSpot {\npublic:\n    virtual double hourlyRate() const = 0;\n    virtual ~ParkingSpot() = default;\n};\n\nclass CompactSpot  : public ParkingSpot { public: double hourlyRate() const override { return 20; } };\nclass LargeSpot    : public ParkingSpot { public: double hourlyRate() const override { return 40; } };\nclass HandicapSpot : public ParkingSpot { public: double hourlyRate() const override { return 0;  } };\n\n// Caller has ZERO knowledge of the subtypes — adding a new spot type\n// touches no existing code (that's OCP falling out of polymorphism).\ndouble billFor(const ParkingSpot& spot, int hours) {\n    return spot.hourlyRate() * hours;\n}" },
          { type: "analogy", text: "A car's pedals are the whole of OOP in one object. Encapsulation: the engine internals are sealed under the hood. Abstraction: 'accelerator' is the contract — you don't care about fuel injection. Inheritance: an EV is still a car, same pedals. Polymorphism: pressing the same pedal burns petrol in one car and drains a battery in another." },
          { type: "memory-trick", text: "A-P-I-E: Abstraction, Polymorphism, Inheritance, Encapsulation. In an LLD round, spend your modelling effort on encapsulation + polymorphism — those two decide whether the design survives a new requirement. Inheritance is the one you should be talking yourself OUT of." },
          { type: "interview", qas: [
            { q: "Why prefer composition over inheritance?", a: "Three reasons. (1) Inheritance is compile-time and single-shot — a class has one base forever; composition is swappable at runtime and n-ary. (2) Inheritance exposes you to the fragile base class problem: a base-class change silently breaks derived classes. (3) It couples you to the base's implementation, not just its contract. Rule of thumb: model is-a with inheritance ONLY when you need polymorphic substitution; model has-a and can-do with composition." },
            { q: "What's the difference between an abstract class and an interface?", a: "An interface is a pure contract — no state, no implementation (C++: all-pure-virtual base; Java: `interface`). An abstract class can carry shared state and default implementations, so it's for 'these subtypes are genuinely related and share machinery'. C++ has no separate keyword — an abstract class with only pure virtuals and no data members IS the interface. Practical rule: interface for capability (Comparable, Serializable), abstract class for a partial implementation of a family (AbstractPaymentGateway with shared retry logic)." },
            { q: "In an LLD round, how do you decide what becomes a class?", a: "Nouns from the requirements are candidate classes; verbs are candidate methods. Then filter: a noun becomes its own class if it (a) has state worth owning, (b) has behavior over that state, or (c) varies independently. `ParkingSpot` and `Ticket` qualify. `SpotColor` doesn't — that's an enum on ParkingSpot. Anything that ends up as pure data with no behavior is a struct/value object, not a class." },
          ]},
        ],
      },
      {
        id: "relationships",
        num: 2,
        title: "Class Relationships — association, aggregation, composition",
        blocks: [
          { type: "eli5", text: "Three ways objects can be connected: they just know each other (association), one holds others but they'd survive without it (aggregation), or one owns others so completely that they die with it (composition)." },
          { type: "para", text: "After you've picked classes, the interview is really about the ARROWS between them. Getting the relationship wrong is what produces designs that leak memory, cascade deletes wrongly, or can't be tested. There are five you need: association, aggregation, composition, inheritance, and dependency — separated by ownership and lifetime." },
          { type: "heading", text: "The ownership spectrum" },
          { type: "para", text: "Association: two objects know about each other, neither owns the other. A Student and a Course reference each other; deleting the course doesn't delete the student. Aggregation: a whole holds parts, but the parts outlive it — a Department holds Employees; close the department, the employees still exist. Composition: the whole exclusively owns the parts and they die with it — an Order owns its OrderLines; delete the order and the lines are meaningless." },
          { type: "pre", text: "// ASSOCIATION — knows about, does not own (non-owning pointer/reference)\nclass Course;\nclass Student {\n    std::vector<Course*> enrolled;   // raw ptr = deliberately non-owning\n};\n\n// AGGREGATION — holds parts that outlive the whole (shared ownership)\nclass Department {\n    std::vector<std::shared_ptr<Employee>> staff;   // employees exist independently\npublic:\n    void add(std::shared_ptr<Employee> e) { staff.push_back(std::move(e)); }\n};\n\n// COMPOSITION — exclusive ownership, parts die with the whole\nclass Order {\n    std::vector<OrderLine> lines;    // by value: lifetime is bound to Order\n    // or std::vector<std::unique_ptr<OrderLine>> if polymorphism is needed\n};\n\n// DEPENDENCY — 'uses' transiently, no stored reference at all\nclass InvoicePrinter {\npublic:\n    void print(const Order& order);  // uses Order only for this call\n};" },
          { type: "heading", text: "Reading the arrows in UML" },
          { type: "para", text: "Association is a plain line (arrow if the direction is one-way). Aggregation is a HOLLOW diamond at the owner end. Composition is a FILLED diamond at the owner end. Inheritance is a hollow TRIANGLE pointing at the base. Dependency is a dashed arrow. Interviewers do read these — a filled diamond where you meant aggregation says the parts get deleted, which is a different design." },
          { type: "heading", text: "Multiplicity" },
          { type: "para", text: "Every relationship carries a multiplicity on each end: 1, 0..1, 1..*, 0..*, or an exact n. `ParkingLot 1 —— 1..* ParkingFloor` reads 'a lot has at least one floor, a floor belongs to exactly one lot'. State these out loud while drawing — it's the fastest way to surface a missing requirement ('can a vehicle hold two tickets at once?')." },
          { type: "analogy", text: "A university: a Student and a Professor are associated (they know each other, both exist alone). A Department aggregates Professors (shut the department, the professors are still employed). A Building composes Rooms (demolish the building and the rooms are gone by definition)." },
          { type: "memory-trick", text: "Ask one question: 'if I delete the whole, should the part die?' Yes and only-this-owner → composition (filled diamond, `unique_ptr`/by-value). Yes-but-shared → aggregation (hollow diamond, `shared_ptr`). No → association (raw pointer/reference, non-owning). Never stored at all → dependency." },
          { type: "common-mistake", text: "Using `shared_ptr` everywhere because it 'just works'. That silently turns every relationship into aggregation and creates reference cycles (A holds B holds A → neither is ever freed). Pick the pointer type from the RELATIONSHIP, not from convenience: `unique_ptr` for composition, `shared_ptr` for genuine shared ownership, `weak_ptr` to break a cycle, raw pointer/reference for non-owning association." },
          { type: "interview", qas: [
            { q: "Aggregation vs composition — give a concrete example where choosing wrong causes a bug.", a: "A Playlist and its Songs. If you model it as composition (Playlist owns Songs by value / unique_ptr), deleting a playlist deletes the songs — but the same song is in three other playlists, so those now dangle or you've silently duplicated the library. It's aggregation: the playlist holds shared references, songs live in the music library and outlive any playlist. Conversely modelling an Order's OrderLines as aggregation leaks — nothing else ever references a line, so nothing ever frees it." },
            { q: "How do you break a circular reference between two classes?", a: "Decide which direction is the owner. The owner keeps a strong reference (`shared_ptr`), the back-pointer becomes `weak_ptr` (or a raw non-owning pointer). Example: Parent owns Children via `shared_ptr`; each Child's `parent` back-reference is `weak_ptr`. Without this the refcounts never reach zero and both leak. In Java/GC languages the leak is less visible but the design smell is identical — and it still breaks serialization with infinite recursion." },
            { q: "What is a dependency relationship and why does it matter for testing?", a: "A dependency is transient use — a class receives an object as a parameter or creates it locally, but stores nothing. It matters because dependencies are the seams where you inject test doubles. If `OrderService` stores a concrete `SmtpMailer` member, it's a composition and you can't test without a mail server. If it takes a `Mailer&` parameter (or gets one injected via constructor), it's a dependency on an abstraction, and the test passes a `FakeMailer`. This is exactly DIP applied to relationships." },
          ]},
        ],
      },
      {
        id: "class-diagrams",
        num: 3,
        title: "Class Diagrams — drawing the design in the interview",
        blocks: [
          { type: "eli5", text: "A box per class, split into three rows: the name, what it stores, what it does. Then lines between the boxes showing who knows whom. That's it — that's the picture the interviewer wants." },
          { type: "para", text: "The class diagram is the deliverable of an LLD round. You will be asked to draw one on a whiteboard or in a shared doc within the first 15 minutes. It's not a documentation exercise — it's how you demonstrate that you can hold a design in your head and communicate it. A clean diagram with 6 classes beats a sprawling one with 20." },
          { type: "heading", text: "The three-compartment box" },
          { type: "pre", text: "+---------------------------------+\n|          ParkingLot             |   <- class name (interface: <<interface>> above it)\n+---------------------------------+\n| - id: string                    |   <- attributes:  visibility name: type\n| - floors: List<ParkingFloor>    |\n| - rateStrategy: RateStrategy    |\n+---------------------------------+\n| + park(v: Vehicle): Ticket      |   <- operations:  visibility name(params): return\n| + unpark(t: Ticket): double     |\n| - findSpot(v: Vehicle): Spot    |\n+---------------------------------+\n\n// Visibility markers\n//   +  public      -  private      #  protected      ~  package\n// Conventions\n//   underlined member  = static\n//   italic name        = abstract class / abstract method" },
          { type: "heading", text: "The five lines you need" },
          { type: "pre", text: "A ---------> B     association   : A knows B (plain arrow, non-owning)\nA <>-------- B     aggregation   : HOLLOW diamond at A; B outlives A\nA <#>------- B     composition   : FILLED diamond at A; B dies with A\nA ----|> B         inheritance   : hollow TRIANGLE at B; A is-a B\nA - - - -> B       dependency    : DASHED arrow; A uses B transiently\n\n// Multiplicity sits at the ends:\n//   ParkingLot 1 <#>------- 1..* ParkingFloor\n//   'one lot owns one-or-more floors; a floor belongs to exactly one lot'" },
          { type: "heading", text: "A worked mini-diagram" },
          { type: "pre", text: "                 <<interface>>\n                  RateStrategy\n                 + rate(h: int): double\n                        /_\\\n                         |  (implements)\n         +---------------+---------------+\n         |                               |\n   FlatRateStrategy              HourlyRateStrategy\n\n\n   ParkingLot 1 <#>---- 1..* ParkingFloor 1 <#>---- 1..* ParkingSpot\n        |                                                   /_\\\n        | (uses)                                             |\n        v                                          +---------+---------+\n   RateStrategy                              CompactSpot         LargeSpot\n\n   ParkingLot ----> Ticket        (association: lot hands out tickets)\n   Ticket     ----> Vehicle       (association: ticket references the vehicle)\n\n// Read it back out loud: 'a lot owns floors, a floor owns spots, spots are\n// polymorphic by size, and the lot delegates pricing to a strategy it does\n// not own.' If that sentence is true, the diagram is right." },
          { type: "heading", text: "How to drive the drawing in a 45-minute round" },
          { type: "para", text: "Sequence that works: (1) clarify requirements and write down 4-6 bullet points; (2) list candidate nouns and circle the ones that own state; (3) draw the boxes with attributes only — no methods yet; (4) draw the relationships and say the multiplicities out loud; (5) add the 2-3 methods per class that the requirements actually demand; (6) only now name any design patterns you've used. Naming patterns before you have boxes reads as buzzword-first." },
          { type: "memory-trick", text: "Diamond points at the OWNER, triangle points at the PARENT. Filled diamond = 'dies with me'. Hollow diamond = 'outlives me'. If you can't remember the rest, get those two right and describe the others in words." },
          { type: "common-mistake", text: "Drawing getters and setters in the operations compartment. They're noise — every reviewer assumes accessors exist. Show only the behavior that expresses the design: `park()`, `calculateFee()`, `assignSpot()`. A compartment full of `getX()/setX()` is a signal that the class is an anemic data bag with the real logic living somewhere else." },
          { type: "interview", qas: [
            { q: "The interviewer says 'just talk, don't draw'. What changes?", a: "Nothing structurally — you narrate the same diagram. 'ParkingLot composes one-to-many ParkingFloor; each floor composes one-to-many ParkingSpot; ParkingSpot is abstract with Compact and Large subtypes; the lot holds a reference to a RateStrategy interface it doesn't own.' Say relationship + multiplicity + ownership for each edge. If you can narrate it in that form, you can draw it, and the interviewer hears the same signal." },
            { q: "Class diagram vs sequence diagram — when does the interviewer want which?", a: "Class diagram answers 'what are the pieces and how are they wired' — it's static structure and it's what 90% of LLD rounds ask for. A sequence diagram answers 'what happens, in order, for this one flow' — objects across the top, time going down, arrows for calls. Reach for it when the interviewer probes a specific interaction ('walk me through what happens when a car exits'), or when the design has non-obvious ordering like a two-phase commit or a retry. Don't volunteer one before the class diagram exists." },
            { q: "How many classes should a good LLD answer have?", a: "For a 45-minute round, 5-8 core classes plus a couple of enums/value objects. Fewer than 4 usually means you've got a God class hiding. More than ~10 means you're modelling things the requirements never asked for, and you'll run out of time before showing any behavior. Start with the minimum that satisfies the stated requirements, then explicitly say 'here's where I'd extend if we needed X' — that shows extensibility thinking without spending time on it." },
          ]},
        ],
      },
    ],
  },
};

export default lldFundamentals;
