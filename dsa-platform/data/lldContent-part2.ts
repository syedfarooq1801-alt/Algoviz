// LLD Part 2: Behavioral Patterns + Classic LLD Problems

const lldPart2 = {
  "design-patterns-behavioral": {
    id: "design-patterns-behavioral",
    title: "Behavioral Patterns",
    chapters: [
      {
        id: "observer",
        num: 1,
        title: "Observer",
        blocks: [
          { type: "para", text: "Define a one-to-many dependency between objects so that when one object (Subject) changes state, all dependents (Observers) are notified and updated automatically. Enables loose coupling — subject doesn't know concrete observer types." },
          { type: "pre", text: "from abc import ABC, abstractmethod\nfrom typing import List\n\nclass Observer(ABC):\n    @abstractmethod\n    def update(self, event: str, data: any): ...\n\nclass Subject:\n    def __init__(self):\n        self._observers: List[Observer] = []\n        self._state = None\n\n    def subscribe(self, o: Observer): self._observers.append(o)\n    def unsubscribe(self, o: Observer): self._observers.remove(o)\n\n    def notify(self, event: str):\n        for o in self._observers:\n            o.update(event, self._state)\n\n    def set_state(self, state):\n        self._state = state\n        self.notify('state_changed')\n\nclass EmailNotifier(Observer):\n    def update(self, event, data):\n        print(f'Email: order state changed to {data}')\n\nclass PushNotifier(Observer):\n    def update(self, event, data):\n        print(f'Push: your order is now {data}')\n\nclass AnalyticsTracker(Observer):\n    def update(self, event, data):\n        print(f'Analytics: tracking {event} with {data}')\n\n# Setup\norder = Subject()\norder.subscribe(EmailNotifier())\norder.subscribe(PushNotifier())\norder.subscribe(AnalyticsTracker())\n\norder.set_state('SHIPPED')  # All 3 notified automatically" },
          { type: "analogy", text: "YouTube subscriptions are Observer pattern. Channel (Subject) doesn't know who subscribed. When video uploads (state change), YouTube notifies all subscribers (Observers). Subscribe/unsubscribe is dynamic. Channel never calls individual subscribers by name." },
          { type: "memory-trick", text: "Observer = 'event bus in a class.' Push model (subject sends data) vs Pull model (observer fetches from subject). Event-driven systems, pub/sub, React hooks (useEffect watching state), Redux store subscriptions — all Observer." },
          { type: "common-mistake", text: "Memory leaks when observers aren't unsubscribed. Subject holds strong reference to observer. If observer (e.g., UI widget) is destroyed but never unsubscribed, it stays alive in subject's list. Always unsubscribe in cleanup/destructor. Java has WeakReference pattern for this." },
          { type: "interview", qas: [
            { q: "Observer vs Event Emitter vs Pub/Sub?", a: "Observer: tight coupling, subject knows Observer interface directly. EventEmitter (Node.js): looser, observers identified by event string. Pub/Sub: decoupled via message broker (Kafka, RabbitMQ) — publisher doesn't know subscriber exists. Same pattern, different coupling levels. Redis pub/sub and Kafka are Pub/Sub. RxJS Observables and React useState watchers are Observer." },
            { q: "How does React implement Observer pattern?", a: "useState setter triggers re-render of all components subscribed to that state (via virtual DOM diffing). useContext notifies all consumers when context value changes. Redux store.subscribe() is explicit Observer. React Query's onSuccess callbacks. SWR's cache invalidation notifications. React is built on Observer at every level." },
          ]},
        ],
      },
      {
        id: "strategy",
        num: 2,
        title: "Strategy",
        blocks: [
          { type: "para", text: "Define a family of algorithms, encapsulate each one, and make them interchangeable. Strategy lets the algorithm vary independently from clients that use it. Replace if-else chains with polymorphic behavior." },
          { type: "pre", text: "from abc import ABC, abstractmethod\n\nclass SortStrategy(ABC):\n    @abstractmethod\n    def sort(self, data: list) -> list: ...\n\nclass QuickSort(SortStrategy):\n    def sort(self, data): return sorted(data)  # simplified\n\nclass MergeSort(SortStrategy):\n    def sort(self, data): return sorted(data, key=None)  # simplified\n\nclass BubbleSort(SortStrategy):\n    def sort(self, data):\n        arr = data.copy()\n        for i in range(len(arr)):\n            for j in range(len(arr)-i-1):\n                if arr[j] > arr[j+1]: arr[j], arr[j+1] = arr[j+1], arr[j]\n        return arr\n\nclass Sorter:\n    def __init__(self, strategy: SortStrategy):\n        self._strategy = strategy\n\n    def set_strategy(self, strategy: SortStrategy):\n        self._strategy = strategy  # swap at runtime!\n\n    def sort(self, data: list) -> list:\n        return self._strategy.sort(data)\n\nsorter = Sorter(QuickSort())\nresult = sorter.sort([3,1,4,1,5])\n\n# Switch strategy based on data size\nif len(data) < 10:\n    sorter.set_strategy(BubbleSort())  # simple for tiny arrays\nelse:\n    sorter.set_strategy(QuickSort())" },
          { type: "memory-trick", text: "Strategy = 'plug-in algorithm.' If you see if-else / switch on a type to pick behavior — that's a Strategy waiting to happen. Each branch becomes a concrete Strategy class. Context holds a reference to current strategy, swappable at runtime." },
          { type: "interview", qas: [
            { q: "Real-world Strategy use cases?", a: "Payment processing: CreditCard, PayPal, Crypto each implement PaymentStrategy. Compression: ZipStrategy, GzipStrategy, BrotliStrategy. Auth: JWTStrategy, SessionStrategy, OAuthStrategy (Passport.js uses Strategy pattern explicitly by name). Routing algorithms: Dijkstra vs A* vs BFS as interchangeable strategies." },
            { q: "Strategy vs State pattern?", a: "Both swap behavior via polymorphism. Strategy: client explicitly chooses and sets the strategy. State: object transitions between states automatically based on internal conditions — client doesn't pick the state directly. Strategy is about HOW to do something. State is about WHAT PHASE the object is in. State strategies often know about each other (transitions). Strategy objects are usually independent." },
          ]},
        ],
      },
      {
        id: "command",
        num: 3,
        title: "Command",
        blocks: [
          { type: "para", text: "Encapsulate a request as an object, allowing you to parameterize clients with different requests, queue operations, log requests, and support undoable operations. Decouples the sender of a request from its receiver." },
          { type: "pre", text: "from abc import ABC, abstractmethod\nfrom typing import List\n\nclass Command(ABC):\n    @abstractmethod\n    def execute(self): ...\n    @abstractmethod\n    def undo(self): ...\n\nclass TextEditor:\n    def __init__(self): self.text = \"\"\n    def insert(self, pos, chars): self.text = self.text[:pos] + chars + self.text[pos:]\n    def delete(self, pos, length): self.text = self.text[:pos] + self.text[pos+length:]\n\nclass InsertCommand(Command):\n    def __init__(self, editor: TextEditor, pos: int, chars: str):\n        self.editor, self.pos, self.chars = editor, pos, chars\n    def execute(self): self.editor.insert(self.pos, self.chars)\n    def undo(self): self.editor.delete(self.pos, len(self.chars))\n\nclass DeleteCommand(Command):\n    def __init__(self, editor: TextEditor, pos: int, length: int):\n        self.editor, self.pos, self.length = editor, pos, length\n        self.deleted = \"\"\n    def execute(self):\n        self.deleted = self.editor.text[self.pos:self.pos+self.length]\n        self.editor.delete(self.pos, self.length)\n    def undo(self): self.editor.insert(self.pos, self.deleted)\n\nclass CommandHistory:\n    def __init__(self):\n        self._history: List[Command] = []\n        self._redo_stack: List[Command] = []\n\n    def execute(self, cmd: Command):\n        cmd.execute()\n        self._history.append(cmd)\n        self._redo_stack.clear()\n\n    def undo(self):\n        if self._history:\n            cmd = self._history.pop()\n            cmd.undo()\n            self._redo_stack.append(cmd)\n\n    def redo(self):\n        if self._redo_stack:\n            cmd = self._redo_stack.pop()\n            cmd.execute()\n            self._history.append(cmd)" },
          { type: "memory-trick", text: "Command = 'request as object.' Key capabilities: queue (delay execution), log (audit trail), undo/redo (store undo logic in command). Whenever you need undo/redo — implement Command pattern. Text editors, graphics tools, transactional systems." },
          { type: "interview", qas: [
            { q: "How would you implement undo/redo in a drawing app?", a: "Command pattern. Every user action (draw stroke, move shape, delete, color change) is a Command object with execute() and undo(). CommandHistory maintains undo stack. Ctrl+Z pops and calls undo(). Ctrl+Y pops redo stack and calls execute(). Each command stores everything needed to reverse itself — the old position, the deleted pixels, the previous color." },
            { q: "Command vs Strategy?", a: "Strategy encapsulates HOW something is done (algorithm). Command encapsulates WHAT should be done (a request/action). Strategy is about selecting behavior. Command is about recording, queuing, or undoing behavior. A text editor uses both: Strategy picks the rendering algorithm, Command records each edit action for undo history." },
          ]},
        ],
      },
      {
        id: "template-method",
        num: 4,
        title: "Template Method",
        blocks: [
          { type: "para", text: "Define the skeleton of an algorithm in a base class, deferring some steps to subclasses. Template Method lets subclasses redefine certain steps without changing the algorithm's structure. The invariant part stays in base; variant parts become hooks." },
          { type: "pre", text: "from abc import ABC, abstractmethod\n\nclass DataProcessor(ABC):\n    # TEMPLATE METHOD — fixed algorithm skeleton\n    def process(self, filename: str):\n        data = self.read_data(filename)   # hook 1\n        parsed = self.parse_data(data)    # hook 2\n        result = self.process_data(parsed) # hook 3\n        self.save_result(result)          # hook 4\n        self.send_report(result)          # concrete — same for all\n\n    @abstractmethod\n    def read_data(self, filename: str): ...\n    @abstractmethod\n    def parse_data(self, raw): ...\n    @abstractmethod\n    def process_data(self, data): ...\n    @abstractmethod\n    def save_result(self, result): ...\n\n    def send_report(self, result):  # optional hook — concrete default\n        print(f'Processing complete: {len(result)} records')\n\nclass CSVProcessor(DataProcessor):\n    def read_data(self, f): return open(f).read()\n    def parse_data(self, raw): return [r.split(',') for r in raw.splitlines()]\n    def process_data(self, data): return [d for d in data if d[0] != 'SKIP']\n    def save_result(self, r): open('out.csv','w').write(str(r))\n\nclass JSONProcessor(DataProcessor):\n    def read_data(self, f): return open(f).read()\n    def parse_data(self, raw): import json; return json.loads(raw)\n    def process_data(self, data): return [d for d in data if d.get('active')]\n    def save_result(self, r): import json; open('out.json','w').write(json.dumps(r))" },
          { type: "memory-trick", text: "Template Method = 'Hollywood principle: don't call us, we'll call you.' Base class controls flow, calls your hooks at the right time. If you see abstract methods called from a concrete method in the base class — that's Template Method. Django's class-based views (get_queryset, get_context_data) are Template Method." },
          { type: "interview", qas: [
            { q: "Template Method vs Strategy?", a: "Both vary behavior via subclassing/delegation. Template Method uses inheritance — algorithm in base, variants in subclasses. Strategy uses composition — algorithm swapped via injection. Template Method is static (fixed at class definition). Strategy is dynamic (swapped at runtime). Prefer Strategy for testability and flexibility. Template Method works well when base class needs tight control of the algorithm's structure." },
          ]},
        ],
      },
      {
        id: "state",
        num: 5,
        title: "State",
        blocks: [
          { type: "para", text: "Allow an object to alter its behavior when its internal state changes. The object will appear to change its class. Replace conditional logic (if state == X then ...) with polymorphic state objects." },
          { type: "pre", text: "from abc import ABC, abstractmethod\n\nclass TrafficLightState(ABC):\n    @abstractmethod\n    def handle(self, light: 'TrafficLight'): ...\n    @abstractmethod\n    def display(self) -> str: ...\n\nclass RedState(TrafficLightState):\n    def display(self): return '🔴 RED — STOP'\n    def handle(self, light):\n        print('Switching: Red → Green')\n        light.set_state(GreenState())\n\nclass GreenState(TrafficLightState):\n    def display(self): return '🟢 GREEN — GO'\n    def handle(self, light):\n        print('Switching: Green → Yellow')\n        light.set_state(YellowState())\n\nclass YellowState(TrafficLightState):\n    def display(self): return '🟡 YELLOW — SLOW'\n    def handle(self, light):\n        print('Switching: Yellow → Red')\n        light.set_state(RedState())\n\nclass TrafficLight:\n    def __init__(self):\n        self._state: TrafficLightState = RedState()\n\n    def set_state(self, state: TrafficLightState):\n        self._state = state\n\n    def tick(self):\n        print(self._state.display())\n        self._state.handle(self)\n\nlight = TrafficLight()\nfor _ in range(6): light.tick()  # cycles Red→Green→Yellow→Red..." },
          { type: "memory-trick", text: "State = 'object morphs with internal state.' Key signal: giant if/switch block checking self.state everywhere — replace with State objects. Each state knows its own transitions. Vending machine, traffic light, order lifecycle, connection state machine." },
          { type: "interview", qas: [
            { q: "When would you use State vs a simple enum + switch?", a: "Small, stable state machine with 3-4 states and simple transitions — enum + switch is fine. Complex state machine: many states, each with different behaviors across multiple methods, states with their own data, transitions that depend on conditions — State pattern. When adding a new state requires touching multiple switch statements across multiple methods — refactor to State. When each state has different behavior for the same operation — State." },
          ]},
        ],
      },
    ],
  },

  "lld-problems-classic": {
    id: "lld-problems-classic",
    title: "Classic LLD Problems",
    chapters: [
      {
        id: "parking-lot",
        num: 1,
        title: "Parking Lot Design",
        blocks: [
          { type: "para", text: "Design a multi-floor parking lot that supports different vehicle types (motorcycle, car, truck), tracks availability, assigns spots, generates tickets, and handles payment. Classic LLD interview problem — tests class hierarchy, enums, and system design thinking." },
          { type: "heading", text: "Classes to Identify" },
          { type: "pre", text: "from enum import Enum\nfrom datetime import datetime\nfrom typing import Optional, List\nimport uuid\n\nclass VehicleType(Enum):\n    MOTORCYCLE = 1\n    CAR = 2\n    TRUCK = 3\n\nclass SpotType(Enum):\n    COMPACT = 1   # motorcycles\n    REGULAR = 2   # cars\n    LARGE = 3     # trucks\n\nclass Vehicle:\n    def __init__(self, plate: str, vtype: VehicleType):\n        self.plate = plate\n        self.type = vtype\n\nclass ParkingSpot:\n    def __init__(self, floor: int, spot_id: str, stype: SpotType):\n        self.floor = floor\n        self.spot_id = spot_id\n        self.type = stype\n        self.is_free = True\n        self.vehicle: Optional[Vehicle] = None\n\n    def can_fit(self, vehicle: Vehicle) -> bool:\n        mapping = {\n            VehicleType.MOTORCYCLE: [SpotType.COMPACT, SpotType.REGULAR, SpotType.LARGE],\n            VehicleType.CAR: [SpotType.REGULAR, SpotType.LARGE],\n            VehicleType.TRUCK: [SpotType.LARGE]\n        }\n        return self.type in mapping[vehicle.type] and self.is_free\n\n    def park(self, v: Vehicle): self.vehicle = v; self.is_free = False\n    def vacate(self): self.vehicle = None; self.is_free = True\n\nclass ParkingTicket:\n    def __init__(self, vehicle: Vehicle, spot: ParkingSpot):\n        self.ticket_id = str(uuid.uuid4())[:8]\n        self.vehicle = vehicle\n        self.spot = spot\n        self.entry_time = datetime.now()\n        self.exit_time = None\n        self.amount = 0.0\n\nclass ParkingFloor:\n    def __init__(self, floor_num: int, spots: List[ParkingSpot]):\n        self.floor_num = floor_num\n        self.spots = spots\n\n    def find_spot(self, vehicle: Vehicle) -> Optional[ParkingSpot]:\n        return next((s for s in self.spots if s.can_fit(vehicle)), None)\n\nclass PricingStrategy:\n    def calculate(self, ticket: ParkingTicket) -> float:\n        hours = max(1, (ticket.exit_time - ticket.entry_time).seconds // 3600)\n        rates = {VehicleType.MOTORCYCLE: 20, VehicleType.CAR: 40, VehicleType.TRUCK: 80}\n        return hours * rates[ticket.vehicle.type]\n\nclass ParkingLot:\n    _instance = None  # Singleton\n\n    def __init__(self, name: str, floors: List[ParkingFloor]):\n        self.name = name\n        self.floors = floors\n        self.active_tickets: dict[str, ParkingTicket] = {}\n        self.pricing = PricingStrategy()\n\n    @classmethod\n    def get_instance(cls, *args):\n        if not cls._instance: cls._instance = cls(*args)\n        return cls._instance\n\n    def park_vehicle(self, vehicle: Vehicle) -> Optional[ParkingTicket]:\n        for floor in self.floors:\n            spot = floor.find_spot(vehicle)\n            if spot:\n                spot.park(vehicle)\n                ticket = ParkingTicket(vehicle, spot)\n                self.active_tickets[ticket.ticket_id] = ticket\n                return ticket\n        return None  # full\n\n    def exit_vehicle(self, ticket_id: str) -> float:\n        ticket = self.active_tickets.pop(ticket_id, None)\n        if not ticket: raise ValueError('Invalid ticket')\n        ticket.exit_time = datetime.now()\n        ticket.amount = self.pricing.calculate(ticket)\n        ticket.spot.vacate()\n        return ticket.amount" },
          { type: "heading", text: "Interview Discussion Points" },
          { type: "interview", qas: [
            { q: "How would you handle concurrent parking requests?", a: "Race condition: two cars attempt to park in the same last spot simultaneously. Solutions: (1) Lock the spot atomically — use database row-level lock or mutex around spot.park(). (2) Optimistic locking — spot has version number, update fails if version changed since read, retry. (3) Reserve first (mark spot reserved, not just free/taken), then confirm. In distributed system: Redis SETNX on spot_id — atomic check-and-set." },
            { q: "How would you add pricing tiers (hourly, daily, monthly)?", a: "Strategy pattern on PricingStrategy. Define interface: calculate(ticket) → float. Implement HourlyPricing, DailyCapPricing (max 24h rate), MonthlyPassPricing. ParkingLot holds current strategy, configurable at startup or per-customer type. Monthly pass holders carry different ticket type that bypasses normal pricing." },
            { q: "How would you track available spots in real-time?", a: "Maintain counters per floor per spot type (AtomicInteger in Java). Increment on vacate, decrement on park. O(1) availability check instead of scanning all spots. For display boards: Observer pattern — display subscribes to lot state changes. Or event-driven: spot.park() fires event, all observers (display, monitoring, analytics) update." },
          ]},
        ],
      },
      {
        id: "lru-cache-design",
        num: 2,
        title: "LRU Cache Design",
        blocks: [
          { type: "para", text: "Design an LRU (Least Recently Used) cache with O(1) get and put. Classic design problem testing data structure knowledge and system thinking. Key insight: HashMap + doubly linked list gives O(1) for both operations." },
          { type: "pre", text: "class Node:\n    def __init__(self, key=0, val=0):\n        self.key = key\n        self.val = val\n        self.prev = None\n        self.next = None\n\nclass LRUCache:\n    def __init__(self, capacity: int):\n        self.capacity = capacity\n        self.cache: dict[int, Node] = {}  # key → Node\n        # Sentinel head/tail — never removed\n        self.head = Node()  # most recently used side\n        self.tail = Node()  # least recently used side\n        self.head.next = self.tail\n        self.tail.prev = self.head\n\n    def _remove(self, node: Node):\n        node.prev.next = node.next\n        node.next.prev = node.prev\n\n    def _add_to_front(self, node: Node):\n        node.next = self.head.next\n        node.prev = self.head\n        self.head.next.prev = node\n        self.head.next = node\n\n    def get(self, key: int) -> int:\n        if key not in self.cache: return -1\n        node = self.cache[key]\n        self._remove(node)       # move to front\n        self._add_to_front(node) # O(1)\n        return node.val\n\n    def put(self, key: int, val: int):\n        if key in self.cache:\n            self._remove(self.cache[key])\n        node = Node(key, val)\n        self.cache[key] = node\n        self._add_to_front(node)\n        if len(self.cache) > self.capacity:\n            lru = self.tail.prev   # least recently used\n            self._remove(lru)\n            del self.cache[lru.key]  # must remove from hashmap too!" },
          { type: "interview", qas: [
            { q: "How would you make LRU thread-safe?", a: "Add a threading.Lock() (Python) or ReentrantLock (Java). Acquire lock in get() and put(). For higher throughput: ConcurrentHashMap + lock-striping (different locks for different buckets). For distributed LRU: Redis has built-in LRU eviction policy (maxmemory-policy: allkeys-lru). Single-machine: Java's Collections.synchronizedMap(new LinkedHashMap()) with accessOrder=true gives thread-safe LRU." },
            { q: "Extend to LFU — what changes?", a: "LFU (Least Frequently Used): evict least-used, not least-recently-used. Need: HashMap(key→node), HashMap(freq→DoublyLinkedList of nodes at that freq), track minimum frequency. get/put: increment node's freq, move from old freq list to new freq list, update min_freq. O(1) all operations. Python: collections.OrderedDict makes the per-freq list easy (ordered insertion = tied LFU evicts least recent)." },
          ]},
        ],
      },
      {
        id: "elevator-system",
        num: 3,
        title: "Elevator System Design",
        blocks: [
          { type: "para", text: "Design an elevator control system for a building with multiple elevators and floors. Tests state machine design, scheduling algorithms, and concurrent request handling." },
          { type: "pre", text: "from enum import Enum\nfrom typing import Set\n\nclass Direction(Enum):\n    UP = 'UP'\n    DOWN = 'DOWN'\n    IDLE = 'IDLE'\n\nclass ElevatorState(Enum):\n    MOVING = 'MOVING'\n    IDLE = 'IDLE'\n    DOOR_OPEN = 'DOOR_OPEN'\n\nclass Elevator:\n    def __init__(self, id: int, total_floors: int):\n        self.id = id\n        self.current_floor = 1\n        self.direction = Direction.IDLE\n        self.state = ElevatorState.IDLE\n        self.destination_floors: Set[int] = set()  # buttons pressed inside\n\n    def add_floor(self, floor: int): self.destination_floors.add(floor)\n\n    def move(self):\n        if not self.destination_floors: self.direction = Direction.IDLE; return\n        next_floor = self._next_stop()\n        if next_floor > self.current_floor:\n            self.direction = Direction.UP\n            self.current_floor += 1\n        elif next_floor < self.current_floor:\n            self.direction = Direction.DOWN\n            self.current_floor -= 1\n        if self.current_floor in self.destination_floors:\n            self.destination_floors.remove(self.current_floor)\n            self.state = ElevatorState.DOOR_OPEN\n\n    def _next_stop(self) -> int:\n        # SCAN algorithm: continue in current direction\n        if self.direction == Direction.UP:\n            ups = [f for f in self.destination_floors if f >= self.current_floor]\n            return min(ups) if ups else max(self.destination_floors)\n        elif self.direction == Direction.DOWN:\n            downs = [f for f in self.destination_floors if f <= self.current_floor]\n            return max(downs) if downs else min(self.destination_floors)\n        return min(self.destination_floors, key=lambda f: abs(f - self.current_floor))\n\nclass ElevatorController:\n    def __init__(self, num_elevators: int, floors: int):\n        self.elevators = [Elevator(i, floors) for i in range(num_elevators)]\n\n    def request_elevator(self, floor: int, direction: Direction) -> Elevator:\n        # Assign nearest idle or same-direction elevator\n        best = min(self.elevators, key=lambda e: (\n            0 if e.state == ElevatorState.IDLE else 1,\n            abs(e.current_floor - floor)\n        ))\n        best.add_floor(floor)\n        return best\n\n    def press_button(self, elevator_id: int, floor: int):\n        self.elevators[elevator_id].add_floor(floor)" },
          { type: "interview", qas: [
            { q: "What scheduling algorithms exist for elevators?", a: "FCFS (First Come First Served): simple but inefficient — elevator zigzags. SCAN (Elevator Algorithm): move in one direction, serve all requests, reverse at end. Best for disk scheduling too. LOOK: like SCAN but reverses when no requests remain in current direction (doesn't go to floor 0). SSTF (Shortest Seek Time First): always serve nearest — can starve distant floors. Real elevators use variants of LOOK with priority for occupied elevator direction." },
            { q: "How to handle emergency situations (fire alarm)?", a: "Observer pattern. FireAlarmSystem is Subject. All Elevator objects are Observers. On fire_alarm event: override normal scheduling, ignore floor buttons, move all elevators to ground floor (floor 1), hold doors open, disable internal floor selection. State machine: EMERGENCY state overrides all others. Requires synchronous notification — broadcast, not async." },
          ]},
        ],
      },
      {
        id: "library-system",
        num: 4,
        title: "Library Management System",
        blocks: [
          { type: "para", text: "Design a library system that handles book catalog, member management, borrowing/returning, reservations, fines, and search. Tests entity relationships, state management, and business rule implementation." },
          { type: "pre", text: "from datetime import datetime, timedelta\nfrom enum import Enum\nfrom typing import Optional, List\n\nclass BookStatus(Enum):\n    AVAILABLE = 'AVAILABLE'\n    BORROWED = 'BORROWED'\n    RESERVED = 'RESERVED'\n    LOST = 'LOST'\n\nclass Book:\n    def __init__(self, isbn: str, title: str, author: str, copies: int = 1):\n        self.isbn = isbn\n        self.title = title\n        self.author = author\n        self.total_copies = copies\n        self.available_copies = copies\n\nclass BookCopy:  # Physical copy, not just Book metadata\n    def __init__(self, book: Book, copy_id: str):\n        self.book = book\n        self.copy_id = copy_id\n        self.status = BookStatus.AVAILABLE\n\nclass Member:\n    def __init__(self, member_id: str, name: str):\n        self.member_id = member_id\n        self.name = name\n        self.active_loans: List['Loan'] = []\n        self.reservations: List['Reservation'] = []\n        self.total_fine = 0.0\n        self.MAX_BOOKS = 5\n\n    def can_borrow(self) -> bool:\n        return len(self.active_loans) < self.MAX_BOOKS and self.total_fine == 0\n\nclass Loan:\n    LOAN_PERIOD_DAYS = 14\n    FINE_PER_DAY = 5.0  # rupees\n\n    def __init__(self, member: Member, copy: BookCopy):\n        self.member = member\n        self.copy = copy\n        self.issue_date = datetime.now()\n        self.due_date = self.issue_date + timedelta(days=self.LOAN_PERIOD_DAYS)\n        self.return_date: Optional[datetime] = None\n\n    def calculate_fine(self) -> float:\n        check_date = self.return_date or datetime.now()\n        if check_date <= self.due_date: return 0.0\n        overdue_days = (check_date - self.due_date).days\n        return overdue_days * self.FINE_PER_DAY\n\nclass Library:\n    def __init__(self):\n        self.catalog: dict[str, Book] = {}  # isbn → Book\n        self.copies: dict[str, BookCopy] = {}  # copy_id → BookCopy\n        self.members: dict[str, Member] = {}\n        self.active_loans: dict[str, Loan] = {}\n\n    def borrow_book(self, member_id: str, isbn: str) -> Optional[Loan]:\n        member = self.members.get(member_id)\n        book = self.catalog.get(isbn)\n        if not member or not book: return None\n        if not member.can_borrow(): return None\n        copy = next((c for c in self.copies.values()\n                     if c.book.isbn == isbn and c.status == BookStatus.AVAILABLE), None)\n        if not copy: return None\n        copy.status = BookStatus.BORROWED\n        loan = Loan(member, copy)\n        member.active_loans.append(loan)\n        self.active_loans[f'{member_id}_{copy.copy_id}'] = loan\n        return loan\n\n    def return_book(self, loan: Loan) -> float:\n        fine = loan.calculate_fine()\n        loan.return_date = datetime.now()\n        loan.copy.status = BookStatus.AVAILABLE\n        loan.member.active_loans.remove(loan)\n        loan.member.total_fine += fine\n        return fine\n\n    def search_books(self, query: str) -> List[Book]:\n        q = query.lower()\n        return [b for b in self.catalog.values()\n                if q in b.title.lower() or q in b.author.lower()]" },
          { type: "interview", qas: [
            { q: "How to handle reservations when a book is unavailable?", a: "Reservation queue per book. Member reserves → added to queue. When book returned: check reservation queue — if non-empty, notify first member (Observer pattern or email service), set status to RESERVED for 24h. If not picked up in 24h, move to next in queue. Member.reservations list + Book.reservation_queue. Priority: earlier reservation date wins." },
          ]},
        ],
      },
      {
        id: "chess-game",
        num: 5,
        title: "Chess Game Design",
        blocks: [
          { type: "para", text: "Design a chess game with piece movement validation, check/checkmate detection, and game state management. Tests class hierarchy (piece types), polymorphism, and complex state validation." },
          { type: "pre", text: "from enum import Enum\nfrom typing import Optional, List, Tuple\n\nclass Color(Enum): WHITE = 'W'; BLACK = 'B'\nclass PieceType(Enum): KING='K'; QUEEN='Q'; ROOK='R'; BISHOP='B'; KNIGHT='N'; PAWN='P'\n\nclass Position:\n    def __init__(self, row: int, col: int): self.row, self.col = row, col\n    def is_valid(self): return 0 <= self.row < 8 and 0 <= self.col < 8\n    def __eq__(self, o): return self.row == o.row and self.col == o.col\n\nclass Piece:\n    def __init__(self, color: Color, ptype: PieceType):\n        self.color = color; self.type = ptype; self.moved = False\n    def get_valid_moves(self, pos: Position, board: 'Board') -> List[Position]:\n        raise NotImplementedError\n\nclass Rook(Piece):\n    def __init__(self, color): super().__init__(color, PieceType.ROOK)\n    def get_valid_moves(self, pos, board):\n        moves = []\n        for dr, dc in [(0,1),(0,-1),(1,0),(-1,0)]:\n            r, c = pos.row + dr, pos.col + dc\n            while 0 <= r < 8 and 0 <= c < 8:\n                p = board.get_piece(Position(r, c))\n                if p is None: moves.append(Position(r, c))\n                elif p.color != self.color: moves.append(Position(r, c)); break\n                else: break\n                r += dr; c += dc\n        return moves\n\nclass Knight(Piece):\n    def __init__(self, color): super().__init__(color, PieceType.KNIGHT)\n    def get_valid_moves(self, pos, board):\n        offsets = [(2,1),(2,-1),(-2,1),(-2,-1),(1,2),(1,-2),(-1,2),(-1,-2)]\n        moves = []\n        for dr, dc in offsets:\n            np = Position(pos.row+dr, pos.col+dc)\n            if np.is_valid():\n                piece = board.get_piece(np)\n                if piece is None or piece.color != self.color: moves.append(np)\n        return moves\n\nclass Board:\n    def __init__(self):\n        self.grid: list[list[Optional[Piece]]] = [[None]*8 for _ in range(8)]\n\n    def get_piece(self, pos: Position) -> Optional[Piece]: return self.grid[pos.row][pos.col]\n    def set_piece(self, pos: Position, piece): self.grid[pos.row][pos.col] = piece\n\n    def move_piece(self, src: Position, dst: Position) -> bool:\n        piece = self.get_piece(src)\n        if piece is None: return False\n        valid = piece.get_valid_moves(src, self)\n        if dst not in valid: return False\n        self.set_piece(dst, piece); self.set_piece(src, None); piece.moved = True\n        return True\n\n    def find_king(self, color: Color) -> Optional[Position]:\n        for r in range(8):\n            for c in range(8):\n                p = self.grid[r][c]\n                if p and p.type == PieceType.KING and p.color == color:\n                    return Position(r, c)\n        return None\n\n    def is_in_check(self, color: Color) -> bool:\n        king_pos = self.find_king(color)\n        opp = Color.BLACK if color == Color.WHITE else Color.WHITE\n        for r in range(8):\n            for c in range(8):\n                p = self.grid[r][c]\n                if p and p.color == opp:\n                    if king_pos in p.get_valid_moves(Position(r,c), self):\n                        return True\n        return False" },
          { type: "interview", qas: [
            { q: "How do you detect checkmate?", a: "Player is in checkmate if: (1) currently in check, AND (2) no valid move removes the check. For each piece of the player, for each valid move of that piece, simulate the move on a board copy, check if still in check. If ALL pieces have ALL moves resulting in check — checkmate. This is O(pieces × moves × board_scan) but board is small. Special case: stalemate — not in check but no valid moves." },
            { q: "How would you support en passant and castling?", a: "En passant: track last move. If last move was a 2-square pawn advance, adjacent pawns can capture en passant for one turn. Store last_move in Game state. Pawn.get_valid_moves() checks last_move for en passant eligibility. Castling: check king/rook not moved (moved flag), no pieces between them, king not in check, king doesn't pass through check. Add castling as special move type. King moves 2 squares, rook teleports." },
          ]},
        ],
      },
    ],
  },
};

export default lldPart2;
